# SandleAPT 공통 작업 지침

이 저장소를 수정하는 AI/작업자는 모델 종류와 관계없이 아래 문서를 먼저 읽는다.

1. `README.md`
2. `docs/AI_WORKFLOW.md`
3. `docs/WORK_STATUS.md`
4. 작업 대상에 해당하는 `docs/archive-v1/` 설계 문서

## 핵심 원칙

- 현재 운영 중인 `https://sandleapt.github.io/`는 별도 요청이 없는 한 그대로 유지한다.
- 차세대 아카이브는 운영 사이트와 분리해 설계·개발하고, 충분히 검증된 뒤 루트를 교체한다.
- 큰 단일 파일에 기능·데이터를 몰아넣지 않는다. 기능, 연도, 자료종류, 도메인별로 가능한 한 잘게 분리한다.
- 다른 AI가 `reserved` 또는 `in_progress`로 표시한 작업 영역은 임의로 수정하지 않는다.
- 작업 시작 전 최신 상태를 읽고, 작업 종료 후 `docs/WORK_STATUS.md`에 반드시 인계 내용을 남긴다.
- 완료 기록 시각은 `YYYY-MM-DD HH:mm:ss KST` 형식으로 남긴다.
- 추정으로 기존 데이터를 덮어쓰지 않는다. 원본·기존 구조·관련 문서를 확인한 뒤 수정한다.
- 데이터 구조가 바뀌면 변경 이유와 호환성 영향을 설계 문서에 기록한다.
- 기존 자료를 새 아카이브에 연결할 때는 가능하면 원본을 복제하지 말고 식별자·참조 관계로 연결한다.
- 각 단계는 담당 AI가 코드·데이터·화면 흐름을 스스로 검증하고 보완한 뒤 다음 단계로 진행한다. 단계마다 사용자 승인을 기다리지 않는다.
- 사용자가 나중에 수정 요청을 하면 담당 AI가 관련 Roadmap 단계와 영향 범위를 판별하고, 새 소번호로 수정 이력을 남긴다.
- 보안·권한·공개 범위·원본 훼손·되돌리기 어려운 구조 변경처럼 사용자 판단이 꼭 필요한 경우에만 진행을 멈추고 확인한다.
- 검증 기준과 결과는 대화가 아니라 저장소 문서와 자동 검증 파일에 남겨 다른 AI도 같은 조건으로 재검증할 수 있게 한다.
- 긴 작업은 완료 시점까지 기다리지 않는다. 소단계 또는 독립적으로 검증 가능한 기능 묶음이 끝날 때마다 `docs/WORK_STATUS.md`를 갱신하고 안전한 체크포인트 커밋을 남긴다.
- **작게 잘라서 한다(사용자 지시 2026-09-01).** 한 번에 하나의 기능 묶음만 하고, 검증·커밋·보고를 마친 뒤 다음으로 넘어간다. 여러 기능을 뒤섞어 오래 작업하면 사용자가 중간에 방향을 바로잡을 수 없다. 세부 규칙은 `docs/AI_WORKFLOW.md` 14절.
- 토큰·도구·접속 문제로 갑자기 중단돼도 이어갈 수 있도록, 다음 행동과 재현 명령을 체크포인트마다 적는다.

## 작업 종료 시 최소 인계 항목

- 무엇을 했는지
- 수정한 파일
- 미완료/주의사항
- 다음 작업자가 바로 할 일
- 완료 시각
- 관련 커밋 SHA(확인 가능한 경우)
- 다른 AI가 바로 실행할 다음 행동과 검증 명령

세부 협업 규칙은 `docs/AI_WORKFLOW.md`를 따른다.
