# SandleAPT 공통 작업 지침

이 저장소를 수정하는 AI/작업자는 모델 종류와 관계없이 아래 문서를 먼저 읽는다.

1. `README.md`
2. `docs/AI_WORKFLOW.md`
3. `docs/WORK_STATUS.md`
4. 작업 대상에 해당하는 `docs/archive-v1/` 설계 문서

## 핵심 원칙

- 현재 운영 중인 `https://sandleapt.github.io/`는 별도 요청이 없는 한 그대로 유지한다.
- 차세대 아카이브는 운영 사이트와 분리해 설계·개발하고, 충분히 검증된 뒤 루트를 교체한다.
- 큰 단일 파일에 기능·데이터를 몰아넣지 않는다. 기능, 연도, 자료종류, 도메인별로 가능한 한 잘게 분리한다.
- 다른 AI가 `reserved` 또는 `in_progress`로 표시한 작업 영역은 임의로 수정하지 않는다.
- 작업 시작 전 최신 상태를 읽고, 작업 종료 후 `docs/WORK_STATUS.md`에 반드시 인계 내용을 남긴다.
- 완료 기록 시각은 `YYYY-MM-DD HH:mm:ss KST` 형식으로 남긴다.
- 추정으로 기존 데이터를 덮어쓰지 않는다. 원본·기존 구조·관련 문서를 확인한 뒤 수정한다.
- 데이터 구조가 바뀌면 변경 이유와 호환성 영향을 설계 문서에 기록한다.
- 기존 자료를 새 아카이브에 연결할 때는 가능하면 원본을 복제하지 말고 식별자·참조 관계로 연결한다.

## 작업 종료 시 최소 인계 항목

- 무엇을 했는지
- 수정한 파일
- 미완료/주의사항
- 다음 작업자가 바로 할 일
- 완료 시각
- 관련 커밋 SHA(확인 가능한 경우)

세부 협업 규칙은 `docs/AI_WORKFLOW.md`를 따른다.
