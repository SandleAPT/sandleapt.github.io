# Sandle Archive v1 — 단계별 작업 계획서

상태: 운영 기준 v1
작성: 2026-09-01

## 0. 운영 규칙

이 문서는 Archive v1 작업의 마스터 순서표다. GPT와 Claude는 작업 시작 시 `AGENTS.md` → `docs/AI_WORKFLOW.md` → `docs/WORK_STATUS.md` → 이 문서 순으로 확인한다.

- 큰 단계는 `1`, `2`, `3`처럼 고정한다.
- 실제 작업은 `1.1`, `1.2`, `1.3`처럼 작은 단위로 진행한다.
- 사용자 피드백 수정도 기존 번호를 덮어쓰지 않고 다음 소번호로 기록한다.
- 각 큰 단계는 사용자 승인 전 다음 큰 단계로 넘어가지 않는다.
- **큰 단계가 끝나고 다음 단계로 넘어가기 전에는 완료한 것 / 남은 것 / 보류한 결정 / 다음 단계 세부작업을 사용자에게 먼저 보여준다.**
- Archive와 충돌하지 않는 별도 예약 업무는 병행할 수 있다.
- 구조·검색 순서·입력 방식·권한 방식 변경은 사용자 확인 대상이다.
- 상태는 `planned / in_progress / review / blocked / done`을 사용한다.
- UI의 표시 순서와 데이터 구조는 분리한다. 화면 구성 변경이 데이터 재이관으로 이어지지 않게 한다.

---

# 1. Archive 탐색·검색 화면 방향 확정

목표: 주민이 처음 들어와 주제를 훑고 검색한 뒤 현재 기준과 과거 기록을 자연스럽게 따라가게 한다.

현재 상태: `done` — 임시 확정

### 1.1 Golden Sample 주제 화면 — `done`

- `/archive-v1/` 시험판 생성
- 헬스장·GX, 작은도서관 샘플
- 현재 기준 → 흐름 → 관련 기록 구조 시험

### 1.2 Archive 첫 화면 — `done`

- 통합검색
- TOP5 샘플
- 최근 추가·변경 기록
- 주제 바로가기

피드백: 인기순위보다 전체 주제를 편하게 훑는 구조가 우선이고, 긴 데이터는 화면을 무한히 늘리지 않아야 함.

### 1.3 1.2 피드백 반영 — `done`

- TOP5 제거 후 랜덤 주제 방식 시험
- 현재 기준 3건 / 주요 흐름 4건 / 관련 기록 6건 기본 미리보기
- 초과 자료 `전체 N건 보기`
- 카드·타임라인·기록 클릭 상세 팝업
- 긴 설명 2줄 안팎으로 제한

피드백: 일부 랜덤 노출이 아니라 전체 주제를 모두 보여주고 찾기 쉽게 정렬하는 편이 좋음.

### 1.4 1.3 피드백 반영 — `done`

- 공개 가능한 전체 주제 표시
- 한국어 가나다순 정렬
- 주제 패널이 과도하게 길어지는 문제 보완

피드백: 주제마다 큰 카드가 아니라 큰 주제명이 한눈에 들어오는 밀도가 좋음.

### 1.5 1.4 피드백 반영 — `done`

- 전체 주제를 **가나다순 텍스트형 버튼 모음**으로 변경
- 설명·화살표·점 아이콘 제거
- 주제명 사이 작은 구분점(`·`)
- 화면 폭에 따라 자연스럽게 줄바꿈
- 각 주제명 클릭 시 기존 주제 상세로 이동

### 1.6 검색 결과안 A — `done`

구성: `현재 기준 → 주요 흐름 → 관련 기록`

- 검색창에서 샘플 주제를 검색하면 A안 전용 결과 화면 표시
- 현재 기준을 먼저, 과거 흐름을 다음, 원자료 목록을 마지막에 배치
- 현재 기준 3건 / 주요 흐름 4건 / 관련 기록 6건 기본 표시
- 개별 항목 상세 팝업과 `주제 전체 보기` 연결
- A안 전용 스타일 `archive-v1/assets/search-a.css`

### 1.7 검색 결과안 B — `done`

구성: `현재 기준 → 핵심 요약 → 타임라인 → 자료 전체`

- 검색 기본 화면을 B안으로 시험
- 같은 검색어에서 `A안 / B안` 즉시 전환 가능
- B안 1단계: 현재 기준
- B안 2단계: 주제 설명, 현재 기준, 최근 흐름, 연결 기록을 바탕으로 핵심 요약
- B안 3단계: 타임라인
- B안 4단계: 자료 전체 목록
- 자료가 많아질 경우 화면 폭주를 막기 위해 미리보기 상한과 `주제 전체 보기` 사용
- B안 전용 스타일 `archive-v1/assets/search-b.css`
- `archive-v1/config/search-layouts.js`에 A/B section 순서와 preview 개수를 별도 설정
- `docs/archive-v1/UI_COMPOSITION_V1.md`에 조립식 UI 원칙 기록

사용자 결정: A/B 최종 선택은 전체 기능 흐름을 더 본 뒤 나중에 결정한다. 1단계에서 특정 검색 레이아웃을 고정하지 않는다.

### 1.8 전체 동작 흐름 검토 — `done`

목표: A/B 취향을 고르는 단계가 아니라 Archive의 기본 동선이 전체적으로 이어지는지 확인한다.

검토 흐름:

`첫 화면 → 전체 주제 → 주제 상세 → 검색 A/B → 개별 기록 상세 → 다시 탐색`

- 미리보기 화면에 `1.8 전체 흐름 점검` 전용 내비게이터 추가
- `첫 화면 / 주제 상세 / 검색 A / 검색 B / 상세 팝업`을 한 번에 시험 가능
- 검토 도구는 `archive-v1/review-1-8.js`, `archive-v1/assets/review-1-8.css`로 분리해 실제 운영 UI와 독립
- A/B 선택은 보류하고 두 화면 모두 유지
- 검색 구성은 데이터가 아니라 설정·renderer 계층에서 교체하는 원칙 유지

### 1.9 1단계 임시 확정 — `done`

- 첫 화면·주제 탐색·검색·상세 동선의 큰 흐름 사용자 승인
- A/B 또는 혼합 레이아웃은 실제 데이터와 다른 기능을 붙여본 뒤 config 변경으로 결정 가능
- 검색 레이아웃 변경이 데이터 재이관을 요구하지 않는 구조 유지
- `/archive-v1/`에 단계 전환용 진행상황 판 추가
  - 1단계에서 완료한 것
  - 지금 결정하지 않아도 되는 것
  - 2단계의 다음 작업
  - 전체 1~7단계 위치

1단계에서 보류한 결정:

- 검색 A/B 최종안 또는 혼합안
- 실제 GA4 관심도 집계 방식의 최종 UI 반영
- 실제 대규모 자료가 들어왔을 때의 검색 기본 표시량

이 항목들은 이후 실제 데이터와 관리기능을 붙여본 뒤 결정해도 되며, 2단계 진행을 막지 않는다.

---

# 2. 관리자 입력·검토 흐름 확정

상태: `in_progress`

### 2.1 관리자 영역 화면 샘플 — `review`

구성:

- 공개 Archive와 분리된 `/archive-v1/admin/` 관리 작업대
- 회의 작성
- 새 자료 등록
- 분류 검토
- 관계 검토
- 발행 대기

구현 내용:

- 공개 화면과 관리 화면을 분리한 별도 화면으로 제작
- 공개 Archive 상단에 `🔒 관리 도구 미리보기` 진입 링크 추가
- 회의 작성은 기존 회의록 시스템을 유지하는 원칙에 따라 `회의 설정 / 동대표 명단 / 안건·발언` 기존 화면으로 연결
- 새 자료 등록은 2.2에서 구현할 최소 입력 항목을 미리 보여주는 자리만 마련
- 분류 검토 / 관계 검토 / 발행 대기는 2.4에서 실제 흐름을 붙이기 전 작업대 위치와 정보 밀도만 시험
- `public / resident / private`는 발행 대기 카드에서 미리 노출하되 실제 권한 동작은 2.3 이후 검증
- 관리 화면 자체는 UI 샘플이며 실제 저장 권한·관리자 인증은 아직 연결하지 않음
- GitHub Pages에서 화면을 숨기는 것만으로 보안을 처리하지 않는다는 경고를 화면에 명시
- 관리 화면 스타일은 `archive-v1/assets/admin.css`, 공개 화면 진입 스타일은 `archive-v1/assets/admin-entry.css`로 분리

사용자 확인 포인트:

- 관리 도구가 공개 Archive와 **별도 화면**으로 분리되는 것이 자연스러운지
- 회의 작성 / 새 자료 등록 / 분류 검토 / 관계 검토 / 발행 대기 다섯 업무가 한 작업대에 보이는 것이 편한지
- 이후 좌측 메뉴나 탭으로 나눌 필요가 있는지
- 공개 화면 상단의 관리 도구 진입 위치가 과하거나 부족하지 않은지

### 2.2 AI 새 자료 등록 — `planned`
- 사용자는 보통 GPT/Claude에게 자료 추가 요청
- AI가 제목·날짜·자료종류·주제·조직·관계 후보 작성
- 바로 공개하지 않고 발행 대기로 저장

### 2.3 공개등급 — `planned`
- `public / resident / private`

### 2.4 분류·관계 검토함 — `planned`
- 애매한 항목만 빠르게 승인·수정

### 2.5 사용자 검토 — `planned`
### 2.6 피드백 반영 및 2단계 확정 — `planned`

---

# 3. 회의록 작성 기능과 Archive 연결

상태: `planned`

### 3.1 현행 회의 작성 구조 확인
- 회의 설정 / 안건 / 발언·결정 / 수기 입력 / 안건별 출력 / 가능한 1페이지 회의록

### 3.2 기존 회의 데이터 → Archive 필드 매핑
- 회의 → Document
- 안건 → Fragment
- 의결 결과 → Action / Relation
- 관련 주제 → Topic

### 3.3 수기 회의 작성 화면 프로토타입
### 3.4 회의 저장 시 Archive 초안 자동 생성
### 3.5 1페이지 출력 검증
### 3.6 사용자 실사용 검토
### 3.7 피드백 반영 및 3단계 확정

---

# 4. 저장소·원본·권한 구조 확정

상태: `planned`

### 4.1 GitHub / Google Drive 역할 분리

GitHub: HTML/CSS/JS, 메타데이터, 검색 텍스트, taxonomy, relation, 검색 인덱스.

외부 원본 저장소: PDF, HWP/DOCX/XLSX, 사진, 영상·음성, 대용량 스캔.

### 4.2 외부 원본 참조 형식
### 4.3 `public / resident / private` 실제 권한 처리
### 4.4 사용자 검토
### 4.5 피드백 반영 및 4단계 확정

---

# 5. 다른 성격의 주제로 구조 재검증

상태: `planned`

### 5.1 검증 주제 2개 선택
후보: 선거·선관위 / 주차 / 잡수입·운영경비 / 하자판결금 / 도서관

### 5.2 첫 번째 주제 샘플
### 5.3 두 번째 주제 샘플
### 5.4 taxonomy / Relation 보정
### 5.5 사용자 검토
### 5.6 데이터 모델 v1 동결

---

# 6. 실제 데이터 소규모 이관

상태: `planned`

기본 상한은 `MIGRATION_V1.md`를 따른다.

### 6.1 첫 Pilot 배치
- 최대 12 작업 포인트
- 원본 최대 8건
- Fragment 최대 30건
- 복잡 문서 최대 2건

### 6.2 Pilot 사용자 검토
### 6.3 Pilot 규칙 보정
### 6.4~ 실제 배치

배치마다 번호를 늘리고 구조변경 또는 일정 배치 누적 시 중간검수 번호를 추가한다.

---

# 7. 기존 포털 기능 통합 및 최종 전환

상태: `planned`

### 7.1 기존 기능 연결
회의록 / 관리비 추적 / 하자판결금 / 업무현황 / 커뮤니티 안내 / 기타 유지 기능

### 7.2 현행 포털에서 Archive 시험판 연결
### 7.3 PC·모바일·검색·관리자·권한·원본 열람 검수
### 7.4 기존 포털 Legacy 보존
### 7.5 최종 사용자 승인
### 7.6 `sandleapt.github.io` 루트 전환

---

# 현재 위치

2026-09-01 기준:

- `1.1`~`1.9` 완료 — **1단계 임시 확정**
- 현재 검토: `2.1 관리자 영역 화면 샘플`
- 다음 예정: `2.2 AI 새 자료 등록`
- A/B 최종 선택은 보류
- Claude 예약 업무는 별도 작업으로 유지

작업 종료 시 `WORK_STATUS.md`에 현재 번호, 완료 번호, 다음 번호, 변경 파일, 완료 시각과 관련 커밋을 남긴다.
