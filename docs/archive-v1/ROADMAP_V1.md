# Sandle Archive v1 — 단계별 작업 계획서

상태: 운영 기준 v1
작성: 2026-09-01

## 0. 운영 규칙

이 문서는 Archive v1 작업의 마스터 순서표다. GPT와 Claude는 작업 시작 시 `AGENTS.md` → `docs/AI_WORKFLOW.md` → `docs/WORK_STATUS.md` → 이 문서 순으로 확인한다.

- 큰 단계는 `1`, `2`, `3`처럼 고정한다.
- 실제 작업은 `1.1`, `1.2`, `1.3`처럼 작은 단위로 기록한다.
- 사용자 피드백 수정도 기존 번호를 덮어쓰지 않고 다음 소번호로 기록한다.
- 큰 단계가 끝나고 다음 단계로 넘어가기 전에는 완료한 것 / 남은 것 / 보류한 결정 / 다음 단계 세부작업을 저장소 체크포인트에 먼저 남긴다.
- 담당 AI가 기능·데이터·호환성 검증과 보완까지 마치면 사용자 중간 컨펌 없이 다음 단계로 진행한다.
- 다만 보안·권한·데이터 모델 변경, 원본 훼손 가능성, 큰 구조변경처럼 되돌리기 어려운 결정은 중간 확인할 수 있다.
- `done`은 코드가 존재하는 시점이 아니라 자동 검증·실제 데이터 검증·인계 기록까지 끝난 상태에 사용한다.
- 사용자가 나중에 피드백을 주면 관련 완료 단계를 새 소번호로 다시 열어 보완하고, 이후 단계의 영향 범위도 재검증한다.
- 소단계 또는 검증 가능한 기능 묶음마다 중간 체크포인트를 커밋한다.
- Archive와 충돌하지 않는 별도 예약 업무는 병행할 수 있다.
- 상태는 `planned / in_progress / review / blocked / done`을 사용한다.
- UI 표시 순서와 데이터 구조는 분리한다. 화면 구성 변경이 데이터 재이관으로 이어지지 않게 한다.

---

# 1. Archive 탐색·검색 화면 방향 확정

현재 상태: `done` — 임시 확정

### 1.1 Golden Sample 주제 화면 — `done`
- `/archive-v1/` 시험판
- 헬스장·GX, 작은도서관 샘플
- 현재 기준 → 흐름 → 관련 기록

### 1.2 Archive 첫 화면 — `done`
- 통합검색 / 최근 기록 / 주제 탐색

### 1.3 긴 데이터·상세 탐색 보완 — `done`
- 기본 미리보기 상한 / 전체보기 / 개별 상세

### 1.4 전체 주제 가나다순 — `done`
### 1.5 전체 주제 텍스트형 버튼 — `done`

### 1.6 검색 결과안 A — `done`
- `현재 기준 → 주요 흐름 → 관련 기록`

### 1.7 검색 결과안 B — `done`
- `현재 기준 → 핵심 요약 → 타임라인 → 자료 전체`
- A/B 전환
- 검색 section 순서·표시량 config 분리

### 1.8 전체 동작 흐름 검토 — `done`
### 1.9 1단계 임시 확정 — `done`

### 1.10 검색 A/B 최종 선택 — `done` (2026-09-02, 사용자 결정)

**B안으로 확정.** 실제 회의록 224건을 붙인 뒤 두 화면을 다 보고 사용자가 정했다 — *“검색해서 들어가서 보이는 화면이 더 마음에 들어.”*

- 첫 화면에서 **주제 이름을 눌러도 같은 화면**이 나오도록 통일했다. 같은 자료를 두 가지 모양으로 보여줄 이유가 없다.
- A안과 주제 상세 화면은 **코드에 남겨 둔다.** 검색 결과의 전환 버튼으로 볼 수 있고, `search-b.js`가 빠져도 `app.js`의 주제 화면이 받아 주므로 화면이 비지 않는다.
- 샘플로 볼 때는 판단이 어려웠고, 실제 자료를 붙이고 나서야 결정할 수 있었다.

보류 가능:
- GA4 관심도 반영
- 대규모 데이터의 기본 표시량

---

# 2. 관리자 입력·검토 흐름 확정

현재 상태: `review` — 실제 사용과 함께 보완 (2.4는 2026-09-01~02 사용자 검토로 크게 손봄)

사용자 요청: 2단계는 소단계마다 확인받지 않고 전체를 먼저 연결한 뒤 한 번에 수정한다.

### 2.1 관리자 영역 화면 — `review`
- 공개 Archive와 분리된 `/archive-v1/admin/`
- 좌측 관리 메뉴

### 2.2 AI 새 자료 등록 흐름 — `review` (테스트 가능)
- 제목 / 자료종류 / 날짜 / 적용범위 / 공개등급 / 원본위치 / 메모
- 예시 채우기 → AI 초안 시뮬레이션 → 검토함 이동
- 실제 AI 호출은 아직 없음

### 2.3 공개 등급 — `review` (테스트 가능)
- `public / resident / private`
- resident/private는 UI 숨김으로 보호하지 않음
- 실제 인증 전 공개 GitHub Pages 빌드에 원본·검색본문을 넣지 않음

### 2.4 분류·관계 검토함 + 발행 대기 — `review` (테스트 가능)
- 분류: 주제 / 조직 / 현행상태 / 신뢰도 / 승인·수정·보류
- 관계: 대상 / 관계종류 / `explicit / verified / inferred` / 승인·생략
- 발행 대기: 공개등급 / 미리보기 / 메모리 프로토타입 발행

**2026-09-01~02 사용자 검토(`4.4`) 피드백으로 크게 손봄 — 아래는 모두 `done`:**

- 2.4a `done` — **관계 문구를 사람이 읽는 말로.** 지적: “관계종류가 이해 안 된다, 영어라서가 아니라 무슨 역할인지 몰라서”. 번역이 아니라 화면의 목적을 설명하는 문제였다. 하자소송 8년 연쇄를 예시로 넣고, 고른 값의 뜻과 저장 결과를 미리 보여준다. `verified`/`inferred`를 ‘확인 작업을 실제로 했는가’로 명문화. `shared/relation-labels.js` (2026-09-01 22:40 KST)
- 2.4b `done` — **자동 판정 도입.** 사용자 결정: 한 건씩 승인받지 말고 자동으로 정하고 이상한 것만 고친다. `shared/auto-assign.js`. 관계는 원문이 실제로 가리킬 때만 만든다(문서 종류만 보고 ‘근거 회의 확인 필요’를 target에 넣던 결함 폐지). (2026-09-01 23:35 KST)
- 2.4c `done` — **실측 후 제목 우선으로 보정.** 실제 안건 1,212건에 돌려보니 제목+본문 동시 채점은 **61.2%가 사람에게** 넘어와 자동화가 무의미했다. 회의록 앱 `autoTags`와 같은 규칙(제목 먼저, 걸리면 본문은 안 봄)으로 바꿔 **제목 판정 92.1%**. 대표 주제 선택 순서까지 분류표 순서로 맞춰 두 앱이 같은 답을 내게 함. (2026-09-02 01:05 KST)
- 2.4d `done` — **주제 훑어보기 화면**(`#topicReview`). 승인 게이트가 아니라 1,212건을 전부 펼쳐 놓고 이상한 것만 고치는 화면. 확인이 급한 순서로 정렬. `shared/topic-review-model.js` + `admin/views/topic-review.js` (2026-09-02 01:40 KST)
- 2.4e `done` — **회의록 수동 태그를 단일 출처로.** 사용자 결정(“태그는 결국 양쪽 다 쓰는 것”). 쓰기 전에 읽기 결함 3건을 잡음: 이미 붙은 수동 태그 250건 무시, 태그가 여러 개인데 하나만 고르게 돼 있던 것, 옛 태그 이름(`소송`·`미화`)이 그대로 뜨던 것. (2026-09-02 02:20 KST)
- 2.4f `done` — **회의록에 태그 쓰기 첫 시험 3건.** 저장·재조회·날짜 유지·앱 화면 반영까지 확인. `date`를 안쪽 `meeting.date`에서 실어 보냄. 검증은 문자열이 아니라 파싱한 내용으로 대조(형식 차이로 오탐). (2026-09-02 03:10 KST)
- 2.4i `done` — **관계 대상 찾기(`shared/reference-resolver.js`).** 관계의 대상이 「공고 제2022-128호」라는 **글자**였다. 그대로 저장하면 눌러도 아무 데도 안 가는 링크가 쌓인다. 원칙은 찾는 것이 아니라 **모르면 만들지 않는 것** — 확정(딱 하나) / 후보(여럿, 사람이 고름) / 없음(만들지 않음). 배포본 14/14 통과. **실측 결과 자동으로 이어지는 것은 0건**(아래 참조). (2026-09-02 05:20 KST)
  - **관계 자동화는 당분간 보류.** 실측: 안건 1,212건 중 원문이 무언가를 가리키는 것이 **4건**뿐이고, 그 4건이 가리키는 「공고 제2022-128호」는 보관함에 없다. 보관함 56건이 대부분 선거·선관위 공고라 관리사무소 일반 공고가 아직 없다. **재료가 없어서 자동화할 대상이 없는 것**이지 규칙이 부실한 것이 아니다.
  - 사용자 판단(2026-09-02): 관계 검토는 사용자가 하기 어려운 작업이라 자동화를 원했으나, 위 실측에 따라 **하자소송 연쇄처럼 사용자가 이미 아는 중요한 흐름 몇 개를 손으로 잇는 쪽**으로 방향을 정했다. 1,212건을 다 잇는 것은 목표가 아니다.
  - 재개 조건: 관리사무소 일반 공고가 보관함에 쌓이면 「공고 제○○호」 참조가 실제로 이어지기 시작한다. 그때 모듈을 그대로 쓴다.
- 2.4h `done` — **서버 쪽 부분 수정(`setTags`)으로 전환.** 레코드 분리 대신 택한 길. GAS v3(배포 버전 5)이 시트의 json 열만 고치므로 왕복이 3회→1회로 줄고(회의당 약 4.3초), 브라우저가 레코드를 다시 만들지 않아 **9/1 날짜 유실이 구조적으로 불가능**해졌다. 서버에 `setTags`가 없으면 옛 길로 되돌아간다. (2026-09-02 04:40 KST)
- 2.4g `done` — **훑어보기 화면에서 회의록으로 저장.** `shared/tag-writer.js`에 규칙을 박아 두고(네트워크를 주입받아 실제 저장 없이 검증), 화면에 「회의록에 저장」 버튼을 붙였다. 배포본 13/13 통과 + 실제 클라우드 저장 확인. 눌러 보고 고친 것 둘: ① 저장 뒤 다시 읽으면 옛 값이 돌아왔다 — 이 화면은 하루 한 번 갱신되는 정적 사본을 읽기 때문(`4.6`). 저장한 값을 화면에 그대로 반영하도록 바꿈 ② 저장이 멈춘 줄 알았는데 Apps Script 지연이었다. 그때 24초였으나 **평상시는 평균 2.3초**(5회 실측)로 회의당 약 8초다. 경과 시간을 표시. (2026-09-02 03:50 KST)

### 2.5 사용자 전체 검토 — `review`
- 실제로 써보면서 불편한 부분을 모아 수정하기로 함
- 3단계 진행을 막지 않음

### 2.6 피드백 일괄 반영 및 2단계 확정 — `planned`
- 3단계 이후에도 실제 사용 중 발견되는 문제를 모아 반영 가능

관련 설계: `docs/archive-v1/ADMIN_WORKFLOW_V1.md`

---

# 3. 기존 회의록 데이터와 Archive 연결

현재 상태: `done` — 3.7 검증·인계 체크포인트 배포 완료

사용자 목표 재확인:

> 새 회의 작성 도구를 만드는 것이 아니라 **지금 만들어진 회의록과 기록을 좀 더 보기 편하게 정리하는 것**이 핵심.

따라서 기존 계획의 `3.3 수기 회의 작성 화면 프로토타입`은 만들지 않는다. 현재 `minutes` 작성 화면을 그대로 원본 시스템으로 유지하고, 저장된 회의록을 읽는 adapter 방식으로 조정한다.

### 3.1 현행 회의 구조 확인 — `done`

실제 `SandleAPT/minutes` 구조 확인:
- `/minutes/data-index.json`
- `/minutes/data-YYYY.json`
- 회의 레코드: `id / name / date / updatedAt / json`
- 내부 state: `meeting / rosters / agendas / cloudId`
- 안건: `title / summary / decision / followup / votes / remarks / tags(category)`
- 기존 회의 작성·저장·미리보기·PDF는 수정하지 않음

### 3.2 기존 회의 데이터 → Archive 필드 매핑 — `done`

- 회의 1건 → `Document`
- 안건 1건 → `Fragment`
- 의결사항 → `Action: decision`
- 후속조치 → `Action: follow_up`
- Fragment → 회의 Document: `part_of / explicit`
- 표결 → 찬성/반대/기권 요약
- 기존 저장 태그가 있으면 우선 사용
- 저장 태그가 없거나 `기타`면 현재 minutes taxonomy 기반 주제 **후보** 생성
- 자동 후보는 사실처럼 확정하지 않고 검토 대상으로 구분

관련 설계: `docs/archive-v1/MEETING_ADAPTER_V1.md`

### 3.3 실제 회의록 선택·변환 화면 — `done`

관리 작업대의 `3. 회의록 → Archive`에서 직접 테스트 가능:
- 실제 `/minutes/` 연도별 정적 데이터 읽기
- 연도 선택
- 입대의 / 임차 필터
- 회의명 검색
- 실제 회의 선택
- 기존 안건 목록과 Archive 변환 결과를 좌우 비교
- 안건별 논의 / 의결 / 후속조치 펼쳐보기

구현 파일:
- `archive-v1/admin/stage3/meeting-source.js`
- `archive-v1/admin/stage3/meeting-adapter.js`
- `archive-v1/admin/views/meeting-import.js`
- `archive-v1/assets/admin-stage3.css`

### 3.4 Archive 검토 흐름 연결 — `done`

- 선택한 회의의 안건 Fragment를 `2.4 분류 검토`로 보낼 수 있음
- 일반 자료와 회의 안건이 같은 검토함을 사용
- 회의록 변환 항목임을 별도 표시
- 기존 minutes 주제 taxonomy 전체를 분류 선택지에 포함
- 같은 회의 안건을 반복 전송할 때 stable id로 중복 추가 방지

현재 전송·승인 상태는 브라우저 메모리 프로토타입이며 원본 minutes 데이터에는 쓰지 않는다.

### 3.5 기존 1페이지 회의록 유지 확인 — `done`

- 회의 작성: 기존 minutes
- 회의 저장: 기존 minutes
- 미리보기 / PDF / 1페이지 출력: 기존 minutes
- Archive: 검색·주제·타임라인용 재배치만 담당

따라서 Archive 화면 구성이 나중에 바뀌어도 원본 회의록과 출력물을 다시 수정할 필요가 없도록 분리한다.

### 3.6 담당 AI 전체 검증 — `done`

직접 볼 경로:

`/archive-v1/admin/#meetingImport`

검증한 것:
- 실제 회의록이 바로 보이는가
- 기존 자료 → Document/Fragment 구조가 직관적인가
- 긴 안건을 접어서 보는 방식이 편한가
- 논의 / 의결 / 후속조치 구분이 읽기 쉬운가
- 저장 태그와 자동 주제 후보의 차이가 이해되는가
- 검토함으로 보내는 과정이 과하지 않은가
- ‘기존 회의록을 그대로 두고 Archive만 정리’한다는 구조가 목표에 맞는가

자동·실제 데이터 검증 결과는 `docs/archive-v1/STAGE3_VALIDATION.md`에 기록한다.

### 3.7 검증 보완·인계 및 3단계 확정 — `done`

- 과거 분류명과 `기타`를 최신 taxonomy 규칙으로 정규화
- 자동 검증 파일 분리
- 실제 회의 213건 / 안건 1,125건 전체 변환 확인
- 자율 검증·중간 체크포인트·다른 AI 인계 규칙 반영
- 체크포인트 배포 완료: 2026-09-01 15:15:51 KST

---

# 4. 저장소·원본·권한 구조 확정

상태: `done` — 4.5에서 배포본 점검·확정 (2026-09-02 17:00 KST). 남은 것: 4.3f(node 없어 막힘)·4.4 사용자 검토(계속 열어 둠)

### 4.1 GitHub / Google Drive 역할 분리 — `done`
- GitHub: HTML/CSS/JS, 메타데이터, 검색 텍스트, taxonomy, relation, 검색 인덱스
- 외부 저장소: PDF, HWP/DOCX/XLSX, 사진, 영상·음성, 대용량 스캔
- **실제 저장소 확정(2026-09-01 21:10 KST, 사용자 결정)**: 사용자 개인 구글 계정의 드라이브. 새 서비스·유료 계정을 만들지 않는다. 용량 제약 없음(5TB 중 약 156GB 사용). 계정 주소·폴더 id는 개인정보라 이 공개 저장소에 적지 않는다 — 사용자 로컬 비공개 메모 참조. 상세는 `docs/WORK_STATUS.md` 「외부 저장소」.
- **실제로는 드라이브가 아니라 ⑨ 비공개 저장소로 풀렸다 (2026-09-02).** 회의록 앱에 이미 있던 별도 GAS 웹앱이 읽기·쓰기를 서버에서 비밀번호로 확인한다. 드라이브를 거치지 않으므로 **`file_id` 참조 방식도, 링크 공개 전환 문제도 지금은 없다.** `D:\산들마을-비공개\` 4건을 전부 옮겼다. 절차는 `minutes/docs/PRIVATE_STORE.md`.
  - 이 방식의 한계는 **항목당 약 8MB**(파일을 시트 셀에 dataURL로 넣는다). 지금 자료는 최대 2.5MB라 여유가 있다.
  - **드라이브 `file_id` 방식은 그 한도에 걸리는 자료가 실제로 생길 때 한다.** 그때 필요한 것: Apps Script `DriveApp` 액션, 폴더 구조, 그리고 비밀번호 확인 후 내려주는 경로(링크 공개 전환은 인증을 무력화하므로 하지 않는다).

### 4.2 외부 원본 참조 형식 — `done`

- provider에 종속되지 않는 `SourceReference v1`
- public 원본만 공개 참조로 투영
- resident/private 외부 `file_id`는 공개 번들에서 제거
- 설계: `docs/archive-v1/STORAGE_AND_ACCESS_V1.md`

### 4.3 public / resident / private 실제 권한 처리 — `done` (4.3f 제외)

- 4.3a `done` — 공개 projection과 resident/private 발행 차단을 관리자 화면·저장 함수에 연결
- 4.3b `done` — 실제 입주민·관리자 인증. **사용자 결정(2026-09-01 19:45 KST): 회의록 앱의 열람용/수정용 비밀번호 재사용**(별도 회원제 없음). `shared/auth-session.js`(서버 검증·24시간 만료·5분 재확인) + `shared/access-control.js`(role→등급 판정). 배포본 9/9 통과. 검증이 "회수된 비밀번호가 24시간 통과" 결함을 잡아 재확인 주기 도입. 설계 `docs/archive-v1/AUTH_V1.md`. (2026-09-01 20:48:15 KST)
  - 화면 연결(로그인 UI·등급별 표시)은 `4.4`, resident/private 원본 전달 서버는 `4.1` 저장소 결정과 함께 별도 진행
- 4.3c `done` — 인증 만료·회수·감사 로그. **서버 배포까지 완료.** (2026-09-02 15:30 KST)
  - **만료·회수는 이미 되고 있었다**(4.3b). 24시간 TTL + 5분마다 서버 재확인. 비밀번호를 바꾸면 모든 기기가 5분 안에 떨어진다. 그리고 **쓰기는 언제나 서버가 판정**하므로 클라이언트 캐시가 남아 있어도 실제 권한이 새지 않는다.
  - 비어 있던 것은 **기록**이었다. 누가 언제 비밀번호를 넣었는지, 틀렸는지가 아무 데도 안 남았다.
  - **기록은 서버에 둔다.** 브라우저에 두면 자기 기기 것만 보이고 지우면 그만이라 의미가 없다. 비밀번호를 확인하는 곳이 GAS뿐이므로 기록도 거기서 남긴다. `auth_log` 시트, 500줄 유지(옛것부터 삭제).
  - **남기는 것**: 시각 / 동작 / 통과 여부(`edit`·`view`·`fail`·`denied`) / 기기 표시.
  - **남기지 않는 것**: 비밀번호. **틀린 비밀번호도 남기지 않는다** — 오타는 대개 진짜와 한두 글자 차이라 모아 두면 비밀번호를 적어 두는 것과 같아진다.
  - **한계를 화면이 함께 말한다**: Apps Script는 요청한 쪽 IP를 알 수 없어 **누구인지는 기록할 수 없다.** 기기 표시는 기기가 스스로 말한 값이라 신원이 아니다. 그래도 실패가 몰리면 찍어보는 중이고, 일하지 않은 시각의 `edit` 통과는 비밀번호가 샌 신호다.
  - **단정하지 않는다.** "있을 수 있어"까지만 말한다. 잘못 단정하면 사용자가 이웃을 의심하게 된다. 「침입·해킹·공격」을 쓰지 않는 것까지 검사로 고정했다(`stage4-auth-log`).
  - 실패 몰림은 **기기별로 나눠 센다.** 서로 다른 기기의 실패가 겹친 것을 한 덩어리로 보면 아무 일 없는데 경고하게 된다. 기준은 10분 안에 5번(오타는 보통 2~3번).
  - 기록 읽기는 **수정용 키만.** 입주민에게 접근 기록을 보여주지 않는다. 판정은 서버가 한다(화면에서 버튼을 감추는 것은 정리일 뿐).
  - 배포본 18/18 통과. 서버가 아직 v3라 `unknown action`을 돌려주고, 화면은 **"서버에 아직 기록 기능이 안 올라갔어"**라고 그대로 알린다(빈 목록으로 두면 "아무 일 없었음"과 구별되지 않는다).

  **서버 배포 완료 (2026-09-02 16:00 KST) — 버전 6.** 사용자 지적("네가 직접 할 수 있는 걸로 아는데")에 따라 AI가 브라우저로 직접 배포했다. **앞으로 GAS 배포를 사용자에게 미루지 않는다.**
  - 배포 ID·웹앱 주소는 그대로라 클라이언트 설정을 고칠 필요가 없다.
  - 확인: `authLog`가 `unknown action` → `admin_required`로 바뀜(= 새 코드가 살아 있음), 기존 `list` 239건 정상, `auth_log` 시트 자동 생성 후 실제로 줄이 쌓임.
  - **덮어쓰기 전에 편집기와 저장소 사본을 대조했다.** 차이는 주석뿐이었지만(배포 경고 2줄 + 별표 표기 1곳), 확인 없이 덮었으면 몰랐을 일이다. 확인 방법: 길이·줄수·문자코드 검사합을 양쪽에서 계산해 비교 → 다르면 줄 길이 배열로 어긋난 줄을 찾는다. 파일 전체를 옮기지 않고도 정확히 대조된다.
  - 전체 파일을 붙여넣는 대신 **바뀐 부분만 적용**했다. 편집기에 있는 것을 잃지 않고, 옮기는 양도 작다. (script.google.com은 CSP로 localhost fetch를 막으므로 파일을 그대로 가져올 수도 없다.)
  - 마지막에 주석 차이까지 없애 **편집기 == 저장소 사본**(검사합 352560504)으로 맞췄다. 다음 대조가 한 번에 끝난다.
  - **auth_log 첫 두 줄은 AI의 점검 기록이다**(`authLog/denied`, `verify/fail` dev=점검). 침입이 아니다.
- 4.3d `done` — 브라우저 검증 러너: Stage 4 spec 4종 UMD 분리 + `/archive-v1/tests/browser/` 러너. 실제 배포본에서 4/4 통과, 회귀 감지 확인. 기존 node 테스트 미변경. (2026-09-01 17:34:20 KST)
- 4.3e `done` — Stage 3 spec(`stage3-source`·`stage3-adapter`) 브라우저 이식. iframe 격리·setup 단계 추가, 배포본에서 6/6 통과. 격리 해제 시 2회차 실패로 격리 필요성 실증. (2026-09-01 18:22:40 KST)
- 4.3g `done` — `stage3-live-data` 브라우저 이식. 배포본에서 7/7 통과(회의 213건·Fragment 1,125건, 안건수 불일치 0·ID 중복 0). 건수는 고정하지 않고 구조 불변조건만 검사. (2026-09-01 19:20:05 KST)
### 4.6 Archive 데이터 신선도 — `done`

`stage3-live-data` 검증 중 발견: Archive는 `/minutes/data-YYYY.json` **정적 샤드**를 읽는데 이 파일은 minutes 봇이 주기적으로 재발행하므로 클라우드보다 뒤처진다. 2026-09-01 기준 클라우드 224건 / 정적 샤드 213건(입대의 1기 11건 미반영, 샤드 생성 07:18 KST).

- 4.6a `done` — Archive 화면에 '언제까지의 자료인지' 표시. (2026-09-02 11:20 KST)
  - 기준값은 **사본을 만든 시각(`generatedAt`)이 아니라 마지막으로 저장된 회의록의 `updatedAt` 최댓값**으로 잡았다. 사본 만들기는 매일 돌지만 바뀐 게 없으면 파일을 새로 쓰지 않아, `generatedAt`은 "언제 확인했는지"를 말해주지 못한다. 마지막 저장 시각을 쓰면 사본 만들기가 며칠 멈춰도 숫자가 자라지 않아 그 자체로 티가 난다.
  - 3일 이상 밀리면 빨간 경고로 바꾼다(하루 주기이므로 이틀까지는 정상).
  - `SandleArchiveBuild.기준문구(iso, now)` + `#dataAsOf`. 검사는 `stage4-archive-build`에 포함.
  - 배포본 확인: "2026년 9월 1일(어제) 저장분까지 반영돼 있어. 오늘 저장한 건 내일 보여."
- 4.6b `done` — 뒤처졌을 때 할 수 있는 것. (2026-09-02 14:10 KST)
  - **강제 갱신 버튼은 만들지 않기로 했다.** 사본은 GitHub Actions가 만들고, 그것을 돌리려면 저장소 쓰기 토큰이 필요한데 공개 페이지에 넣을 수 없다. **누르면 아무 일도 안 하는 버튼이 제일 나쁘다.**
  - 대신 「지금 클라우드에 물어보기」. `list`는 요청 1회에 메타데이터만 오므로 싸다(본문 없음). 몇 건이 빠졌는지 정확히 말하고, **빠진 회의를 회의록 앱에서 바로 여는 링크**를 준다.
  - 이것이 요점: 사본 갱신은 강제할 수 없어도 **사용자가 원하는 결과 — 그 회의를 지금 보는 것 — 은 다른 길로 줄 수 있다.** 회의록 앱은 클라우드를 직접 읽으므로 늘 최신이다.
  - 평소 방문에는 아무 요청도 하지 않는다. 눌렀을 때만. 안 그러면 정적 사본을 쓰기로 한 이유가 사라진다.
  - 접속 설정은 회의록 앱 코드(`cloud.js`)에서 **직접 뽑아 쓴다.** 베껴 두면 저쪽이 바뀌었을 때 조용히 `unauthorized`가 나는 상태가 된다. 못 뽑으면 박아둔 값으로 되돌아간다.
  - 못 물어봤을 때는 못 물어봤다고 말한다. 조용히 '최신'처럼 보이면 안 된다.
  - `shared/freshness.js` + `freshness-ui.js`, 검사 `stage4-freshness`(fetch 주입 — 러너가 클라우드를 두드리지 않는다).
  - **눌러보고 잡은 것**: 시스템 레코드(`notices_v1`·`checks_v1`)를 회의로 세어 **공고·점검 5건을 "빠진 회의"라고 내놨고, 열리지도 않는 링크까지 줬다.** 종류를 낡은 문서(`DATA.md` §2)만 보고 짐작한 탓이다. 실제 기준은 `minutes/assets/js/app/cloud.js isSysRecord`와 `scripts/build-data.mjs isSystemRecord` 두 곳에 이미 있었다. **종류가 늘면 세 곳을 함께 고칠 것.**
  - 함께 고침: 「…오늘 저장분까지 반영돼 있어. 오늘 저장한 건 내일 보여」가 기준이 오늘일 때 앞뒤가 어긋났다 → 「그 뒤에 저장한 건 내일 보여」.
  - 배포본 확인: 사본 224건 · 클라우드 224건 → "클라우드와 같아. 빠진 회의 없어."

※ 데이터 손실이 아니라 지연이다. 다음 재발행 후 건수가 늘어난 채 통과하는 것이 정상이며 회귀로 오판하지 않는다.
- 4.3f `planned` — node 환경에서 기존 `*.test.js`가 spec을 재사용하도록 통합(케이스 이중 관리 해소)
- 검증: `docs/archive-v1/STAGE4_VALIDATION.md`
### 4.4 사용자 검토 — `in_progress` (2026-09-01 시작)

사용자가 실제로 화면을 보며 지적하고, 그 자리에서 반영하는 방식으로 진행 중이다. 나온 지적과 처리는 `2.4a`~`2.4f`에 있다. 아직 안 본 화면이 남아 있어 계속 열려 있다.

#### 4.4a 공개 화면 로그인 자리 — `done` (2026-09-02 11:50 KST)

권한 규칙(`access-control`)과 세션 판정(`auth-session`)은 만들어 검증까지 했는데, **입주민이 실제로 비밀번호를 넣는 자리가 없었다.** 규칙은 있는데 쓸 수 없는 상태였다. `/archive-v1/`에 `login.js` + `#loginSlot`으로 붙였다.

- 비밀번호는 회의록 앱과 같은 것을 쓰고, 검증은 GAS가 한다(값은 클라이언트에 없다).
- **지금 로그인해도 보이는 자료는 늘지 않는다.** 내부공개·비공개 자료를 내려주는 경로가 아직 없기 때문(4.1). 그 사실을 화면에 그대로 적었다 — "로그인했는데 왜 똑같지?"로 두는 편이 더 나쁘다.
- 사고와 교훈 2건:
  1. `signIn`은 역할 문자열이 아니라 `{ok, role}`을 돌려주는데 그 값을 참/거짓으로 봤다. 객체는 언제나 참이라 **틀린 비밀번호에도 로그인된 것처럼 그려졌다.** 권한 화면이 실패를 성공처럼 보이는 쪽으로 틀린 것이라 제일 나쁜 종류다. `SandleLogin.역할()`로 떼어내고 `stage4-login` 검사를 붙였다.
  2. 파일을 고쳐놓고 `index.html`의 `?v=`를 안 올려서 배포 후에도 브라우저가 옛 파일을 썼고, 고친 버그가 여전히 보였다. **JS를 고치면 캐시 키를 함께 올린다.**
- 둘 다 배포본에 직접 틀린 비밀번호를 넣어보다가 잡혔다. 코드만 읽었으면 못 봤다.

- 검토한 것: 2.4 관계 검토, 2.4 분류(→ 주제 훑어보기)
- 아직 안 본 것: 2.3 공개 등급, 4 저장·권한, 3 회의록 → Archive, 발행 대기

### 4.5 피드백 반영 및 4단계 확정 — `done` (2026-09-02 17:00 KST)

요약을 쓰는 일이 아니라 **배포본을 실제로 점검하고 어긋난 것을 고치는** 일로 했다.

#### 점검한 것 — 4단계의 핵심 안전 속성

이 단계 전체가 지키려는 약속은 하나다. **공개된 곳에 민감한 것이 없어야 한다.** 코드를 읽어 확인하지 않고 실제 파일과 배포본을 훑었다.

| 검사 | 결과 |
|---|---|
| 두 공개 저장소에 계정 주소·드라이브 폴더 id | 없음 |
| 두 공개 저장소의 Apps Script 주소 | 공개 백엔드 **1개뿐**. 비공개 저장소 주소는 없음 |
| 공개 데이터의 휴대폰·주민번호 | 없음 |
| 공개 데이터의 일반 전화 | 1건 — **관리사무소 대표번호**, 공고 원문에 이미 공개된 것 |
| 공개 데이터에 박힌 이미지(dataURL) | 없음 |
| `system-backup.json`의 `memo` 필드 | 0건 (민감 메모가 새어나갈 자리) |
| `roster_history` 가 담은 것 | 이름·동·직책·시기. **호수·연락처·사진 없음** — `DATA.md`의 "공고에 공개된 범위만" 규칙과 맞음 |
| 공개 번들의 자료 등급 | 주제 39개 **전부 `public`** |
| 역할별 열람 범위 | 비로그인 `public` / 입주민 `public·resident` / 관리자 전부 — 판정 함수와 일치 |
| 브라우저 검사 | **18/18 통과** |
| 공개 화면 콘솔 오류 | 없음 |
| 관리 작업대 | 정상(6개 화면 진입 확인) |

#### 고친 것 — 문서·화면이 현실과 어긋나 있었다

- **관리 작업대의 「파일 원본 = 외부 저장소」 안내가 틀려 있었다.** 실제 민감 원본은 드라이브가 아니라 ⑨ 비공개 저장소에 들어갔다. 화면 문구를 바꿨다. 안 고쳤으면 다음 사람이 드라이브를 뒤졌을 것이다.
- **4.1의 「남은 것」이 이미 다르게 풀린 문제였다.** 드라이브 `file_id` 참조는 미해결 과제가 아니라 **지금은 필요 없는 것**이고, 8MB 한도에 걸릴 때 하면 된다. 그렇게 고쳐 적었다.
- 4.3·4.6의 머리 상태줄이 하위 항목과 어긋나 있던 것도 맞췄다.

#### 4단계에서 정한 것 (되짚을 때 볼 것)

1. **별도 회원제를 만들지 않는다.** 회의록 앱의 열람용·수정용 비밀번호를 그대로 쓴다(사용자 결정).
2. **판정은 언제나 서버가 한다.** 화면에서 감추는 것은 정리일 뿐 보호가 아니다.
3. **공개 번들에는 `public`만 넣는다.** 숨기는 것이 아니라 **아예 만들지 않는다.**
4. **모르면 만들지 않는다** — 눌러도 아무 데도 안 가는 링크, 없는 기능을 있는 척하는 버튼을 만들지 않는다(4.6b에서 강제 갱신 버튼을 안 만든 이유).
5. **못 하는 것은 못 한다고 화면에 적는다.** 로그인해도 자료가 안 늘어나는 것, 사본이 하루 늦는 것, 기록이 누구인지는 못 밝히는 것 — 전부 화면에 적혀 있다.
6. **단정하지 않는다.** 인증 기록은 "있을 수 있어"까지만 말한다.

#### 남은 것

- **4.3f** — node가 없어 막혀 있다. 브라우저 러너로 돌고 있어 실무에 지장은 없다.
- **4.4 사용자 검토** — 계속 열어 둔다. 사용자가 화면을 보다 지적하면 새 소번호로 붙인다.
- 드라이브 `file_id` 방식 — 8MB 한도에 걸리는 자료가 생기면.

**4단계는 여기서 닫는다.** 5단계로 넘어갈 수 있다.

---

# 5. 다른 성격의 주제로 구조 재검증

상태: `in_progress` (2026-09-02 시작)

**계획을 하나 바꿨다.** 원안은 "주제 샘플을 만든다"였지만, 1단계 샘플은 손으로 쓴 것이었고 지금은 **실제 안건 1,212건**이 있다. 새 샘플을 만드는 것보다 **실제 자료를 화면에 통과시켜 무엇이 깨지는지 보는 것**이 훨씬 강한 검증이다. 그래서 5.2·5.3은 "샘플 만들기"가 아니라 "실제 자료로 열어 보고 깨진 것 고치기"로 한다.

### 5.1 검증 주제 2개 선택 — `done` (2026-09-02)

건수와 기간을 먼저 재고 골랐다. 짐작으로 고르지 않았다.

1단계 Golden Sample(커뮤니티센터 68건 · 도서관 20건)은 **시설 하나가 꾸준히 굴러가는** 성격이었다. 그래서 성격이 정반대인 극단 두 개를 골랐다.

| 고른 주제 | 건수 | 기간 | 무엇을 흔드는가 |
|---|---|---|---|
| **계약·입찰** | **230건**(2위의 2배) · 회의 130회 | 11년 | 하나의 흐름이 아니라 **십여 개 계약이 나란히 도는 것**. "지금 기준 → 흐름" 모양이 성립하는지 |
| **하자·소송** | 29건 | 11년(2016.06~2026.08) | 건수는 적지만 **한 사슬이 11년에 걸쳐 이어짐**. 외부 기관(LH·신대한·법원)과 회의록 밖 문서가 얽힘 |

#### 5.1 결과 — 공개 화면이 아직 개발 비계를 쓰고 있었다

주제를 열어 보려다 먼저 걸렸다. **실제 내용이 화면 위에서 1,000px 아래**에 있었다. 위쪽을 「1.8 전체 흐름 점검」 판과 「현재 작업 위치」 단계 진행 판이 차지하고 있었다.

더 나쁜 것은 그 문구가 **지금은 사실이 아니었다**는 점이다.
- 「A/B 최종 선택은 보류」 → 1.10에서 B안으로 정했다.
- 「실제 입주민·관리자 로그인은 아직 제공하지 않아」 → 4.4a에서 붙였다. **바로 위에 로그인 칸이 있는데** 없다고 적혀 있었다.
- 「최근 추가·변경 기록 / 샘플 자료」 → 실제 회의록이다.
- 헤더 배지 「4.3 공개 정책 연결」, 하단 「Roadmap 4.3」 공지도 개발 문구였다.

→ 두 판을 공개 화면에서 뺐다(파일은 남기고 주석 처리 — 진행 상황의 진본은 이 문서다). 나머지 문구도 방문자에게 쓸모 있는 말로 바꿨다. **첫 화면에서 전체 주제와 최근 기록이 바로 보인다.**

### 5.2 하자·소송 — `in_progress`

#### 고친 것

- **「가장 최근 흐름」이 가장 오래된 것을 가리켰다.** 화면에 `2016.06`이 '최근'으로 나왔다. 타임라인의 **마지막 칸**을 집는 코드였는데, 손으로 쓴 샘플은 오래된 것이 먼저였고 실제 자료는 최신이 먼저다. 순서에 기대지 않고 **날짜가 가장 큰 것**을 고르게 바꾸고 검사(`stage5-summary`)로 고정했다. 정렬이 또 바뀌어도 안 틀린다.
- **「현재 확인 가능한 기준 N건」** → 규약·계약을 아직 안 붙여서 거기 있는 것은 그냥 최근 안건이다. '기준'이라 부르면 **지나간 안건을 지금 유효한 규정처럼** 읽게 된다. 자료가 주는 이름을 쓰게 바꿨다.
- 「SEARCH RESULT B · 1.7」 개발 라벨 → 「주제로 모아 보기」.

#### 아직 안 고친 것 (5.4에서 다룬다)

- **「기타 안건」이 목록을 채운다.** 하자·소송 29건 중 2025~2026년에만 4건이 연속으로 제목이 「기타 안건」이다. 제목만 보면 무엇인지 알 수 없다. 사용자가 이미 말한 원인 — *"회의할 때 나눠서 했어야 했는데 하나에 다 몰아넣은 적이 많더라고."*
- **왜 이 주제에 걸렸는지 안 보인다.** 2026.05 「기타 안건」은 하자·소송으로 분류됐는데 미리보기 본문은 **커뮤니티센터 위탁업체** 이야기로 시작한다. 방문자는 "이게 왜 하자·소송이지?" 한다. 본문 어느 대목 때문에 걸렸는지 보여줘야 한다.

### 5.3 계약·입찰 — `in_progress`

#### 드러난 것 — 이 주제는 "하나의 흐름"이 아니다

「자료 전체」 위에서부터: 전기안전 업체 / 커뮤니티센터 위탁 / 외부회계감사 / 승강기 유지관리 / 소독 용역 / 기계설비 성능점검 … **서로 무관한 계약 예닐곱 건이 날짜순으로 섞여 있다.**

"우리 단지 승강기 유지관리 누가 하지?"라고 묻는 사람에게 **11년치 모든 계약을 시간순으로** 보여주는 셈이다. 커뮤니티센터·도서관처럼 대상이 하나인 주제에서는 안 보이던 문제다.

→ **모델에 「무엇에 대한 것인가(대상)」 축이 없다.** 지금은 주제 하나에 안건이 평평하게 달려 있을 뿐이다. 5.4에서 다룬다.

#### 같은 안건이 두 번 나온다

`2026.05 외부회계감사 업체 선정의 건`(입대의·임차), `2026.01 승강기 유지관리업체 선정 건`(입대의·임차), `2025.12 소독 용역업체 선정 건`(입대의·임차) — **입주자대표회의와 임차인대표회의가 같은 사안을 각각 다뤄서** 목록에 두 번 뜬다. 기록상으로는 서로 다른 회의가 맞지만, 찾는 사람에게는 한 사안이다.

### 5.4 taxonomy / Relation 보정 — `in_progress`

#### ① 「대상」 축 — `done` **새 축을 만들지 않기로 했다** (2026-09-02 19:00 KST)

먼저 제목에서 대상을 뽑아 새 축을 만들어 봤다. 제목이 `<대상> 업체 선정 건` 꼴로 아주 규칙적이라 **95%가 뽑혔다.** 그런데 **서로 다른 대상이 90개**로 갈라졌다 —

> 승강기 유지관리 / 승강기 유지보수 · 청소 / 청소용역 · 재활용 / 재활용품 수거 · 전기안전 직무고시 / 전기직무고시

같은 것이 해마다 다른 이름으로 적힌 것이다. 그대로 쓰면 「승강기 유지관리」를 눌러도 「승강기 유지보수」 건은 안 나온다. **지금보다 나빠질 수 있다.** 별칭표를 90개 만들 수도 있지만, 관리할 어휘가 하나 더 늘고 해가 지나면 또 어긋난다.

다시 재보니 **계약·입찰 230건 중 225건(98%)이 이미 다른 주제에도 걸려 있었다.**

| 갈래 | 건수 | | 갈래 | 건수 |
|---|---|---|---|---|
| 청소·미화 | 34 | | 통신·전산 | 18 |
| 전기·설비 | 23 | | 보험 | 17 |
| 승강기 | 22 | | 급수·배수·난방 | 16 |
| 화재·소방 | 21 | | 재활용·폐기물 | 15 |
| 경비·보안 · 방역·소독 | 19 | | 회계·결산 | 10 |

**「무엇에 대한 것인가」는 이미 기존 분류 안에 있었다.** 새 어휘도, 새 모델 차원도 만들지 않는다. → **5.6 동결에 영향 없음.**

붙인 것: 주제 화면에 「무엇에 대한 것인지로 좁히기」 줄. 2건 이상인 갈래만, 많은 순으로. 다른 주제로 옮기면 좁힘은 자동으로 풀린다(그 주제엔 없는 갈래일 수 있어서).

배포본 확인 — **계약·입찰 → 승강기 → 22건**, 목록이 전부 승강기 계약이다. 이름이 갈렸던 「유지관리」·「유지보수」·「미디어보드」가 **한자리에 모인다.** 제목에서 뽑는 방식이었으면 갈라졌을 것들이다.

`archive-build.js`의 `갈래` + records 7번째 자리, 화면은 `search-b.js`. 검사는 `stage4-archive-build`에 포함.

#### 아직 남은 것

2. **같은 사안의 입대의·임차 안건 묶기** — `2026.05 외부회계감사 업체 선정의 건`이 두 번 뜬다.
3. **「기타 안건」 제목 처리** — 좁혀 놓은 승강기 22건 안에도 「기타 안건」이 섞여 있다. 본문에서 무엇을 다뤘는지 뽑아 보여줄 수 있는가.
4. **왜 이 주제에 걸렸는지 표시** — 걸린 대목을 보여준다. 3번과 같은 처방일 가능성이 높다.

### 5.5 사용자 검토 — `in_progress`

#### 사용자 지적 — "왜 기존 「주제별 모아보기」가 더 보기가 좋을까. 배치의 문제인가?" (2026-09-02)

**배치 문제가 아니다. Archive에 정보가 없다.** 회의록 앱 ③ 주제별 보기와 나란히 놓고 확인했다.

| | 회의록 앱 ③ | Archive |
|---|---|---|
| 첫 화면 주제 목록 | **건수 순** + 건수 표시 (계약·입찰 230 … 제설·동절기 4) | **가나다순**, 건수 없음 |
| 회의체 | 전체 1212 / 입대의 736 / 임차 476 을 맨 위에 | 없음 |
| 주제를 열면 | **「현재 상태」 한 줄** + **「요점」 연도별 3~4줄** | 「29건 · 2016.06 ~ 2026.08」 |
| 지난 기록 | **연도별로 접힘.** 최근 연도만 펼치고 옛 연도는 `09 정기 · 07 정기 · 06 정기` 한 줄 | 최신 6건만 자르고 나머지는 숨김 |

**핵심**: 회의록 앱은 **답을 먼저** 준다 — *「승강기 / 현재 상태: 신규 업체 관리 체제, 반복 고장 호기 원인 보고 대기」*. Archive는 **질문을 먼저** 한다 — *"무엇을 찾아볼까?"* 그리고 요약이라며 내놓는 것이 건수와 기간뿐이다. **숫자는 정보가 아니다.**

**내 잘못이 하나 있다.** 회의록 앱에는 주제마다 사람이 쓴 「주제 흐름 요약」(`topic_summaries`)이 이미 있다. Archive는 그것을 **시스템 레코드라며 걸러내기만 했다**(4.6b `회의인가`). 건수 세기에서 빼는 것은 맞지만 **내용까지 버린 것이 잘못**이다. Archive가 가장 아쉬워하던 「현재 상태」가 거기 있었다.

읽을 수 있다: `system-backup.json`(같은 origin 공개 파일)에 `topic_summaries_v1` + 조각 8개로 들어 있고, 구조는 `{topics: {"__all__": {text}, "승강기": {text}, …}}`. **새 저장소도 새 통신도 필요 없다.**

처리 순서(5.5a~5.5c):
1. **주제 흐름 요약 붙이기** — 「현재 상태」와 「요점」을 주제 화면 맨 위에. 기계가 만든 「29건 · …」을 대신한다.
2. **주제 목록을 건수 순으로 + 건수 표시** — 한눈에 이 단지가 무엇을 많이 다루는지 보이게. 가나다순은 찾을 이름을 이미 아는 사람에게만 쓸모 있다.
3. **연도별 접기** — 11년치가 한 화면에 들어오게.

#### 소송 안건 공개 범위 — **사용자 결정: 그대로 둔다** (2026-09-02)

물어본 것: 하자·소송에 「입주자대표회의 민사소송 진행 건」이 있고 본문에 *"대상: 입주민 ○○○ · 2022년부터 반복·지속적인 민원 제기"* 같은 내용이 있다. 이름은 가려져 있고 회의록 자체는 이미 공개지만, Archive는 이것을 **주제별로 모아 검색되게** 만든다. 모으면 노출 성격이 달라지지 않겠나.

사용자 답: **"이미 공개된 회의록은 개의치 마."**

→ **회의록에 이미 공개된 것은 Archive에서도 그대로 공개한다.** 주제별로 모인다는 이유로 따로 가리거나 등급을 내리지 않는다. 이 판단은 앞으로 같은 종류의 질문에도 적용한다 — **원본이 공개인 것을 Archive가 다시 검열하지 않는다.**

다만 **새로 들어오는 자료**는 다르다. 회의록에 없던 자료(민원 회신, 소송 서류 등)를 Archive에 붙일 때는 등급을 정해야 하고, 그때는 여전히 확인한다.

### 5.6 데이터 모델 v1 동결 — `planned`
5.4가 끝나야 동결할 수 있다. 「대상」 축을 넣을지 말지가 모델 구조를 바꾸기 때문이다.

---

# 6. 실제 데이터 소규모 이관

상태: `planned`

기본 상한은 `MIGRATION_V1.md`를 따른다.

### 6.1 첫 Pilot 배치
- 최대 12 작업 포인트
- 원본 최대 8건
- Fragment 최대 30건
- 복잡 문서 최대 2건

### 6.2 Pilot 사용자 검토
### 6.3 Pilot 규칙 보정
### 6.4~ 실제 배치

배치마다 번호를 늘리고 구조변경 또는 일정 배치 누적 시 중간검수 번호를 추가한다.

---

# 7. 기존 포털 기능 통합 및 최종 전환

상태: `planned`

### 7.1 기존 기능 연결
회의록 / 관리비 추적 / 하자판결금 / 업무현황 / 커뮤니티 안내 / 기타 유지 기능

### 7.2 현행 포털에서 Archive 시험판 연결
### 7.3 PC·모바일·검색·관리자·권한·원본 열람 검수
### 7.4 기존 포털 Legacy 보존
### 7.5 최종 사용자 승인
### 7.6 `sandleapt.github.io` 루트 전환

---

# 현재 위치

2026-09-01 기준:

- 1단계: `done` — 임시 확정
- 2단계: `review` — 직접 테스트 가능, 실사용과 함께 보완
- 3단계: `done` — 검증·인계 체크포인트 배포 완료
- 완료: `4.1 저장소 역할 분리`, `4.2 SourceReference v1`, `4.3a 공개 발행 경계`
- 현재: `4.3b 실제 입주민·관리자 인증 결정 대기`
- 다음: 사용자 결정 전까지 resident/private 공개 제외 유지, 4.4에서 현재 정책 화면 검토
- 검색 A/B 최종 선택은 보류
- Claude 예약 업무는 별도 작업으로 유지하며 수정하지 않음

작업 종료 시 `WORK_STATUS.md`에 현재 번호, 완료 번호, 다음 번호, 변경 파일, 완료 시각과 관련 커밋을 남긴다.
