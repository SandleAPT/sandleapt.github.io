# 공동 작업 현황

마지막 갱신: 2026-09-01 14:02:45 KST

> GPT와 Claude가 번갈아 작업할 때 가장 먼저 확인하는 파일이다. 과거 상세 로그는 Git 커밋 이력에서 확인할 수 있다.

## 활성 작업

### MINUTES-20260901-01 — 입대의 데이터 대기 9건

- 담당: Claude
- 상태: `blocked` + `reserved`
- 재개 예상: 2026-09-01 14시경
- 범위: 현재 입대의 데이터 9건 후속 작업
- 잠금: 해당 9건과 직접 관련된 데이터/분류 작업
- GPT 원칙: Claude가 완료 또는 담당 해제를 기록하기 전에는 이 9건을 수정하지 않는다.

### ARCHIVE-20260901-01 — Sandle Archive v1

- 담당: GPT
- 상태: `review`
- 마스터 계획: `docs/archive-v1/ROADMAP_V1.md`
- 1단계: `done` — 임시 확정
- 2.1~2.4: `review` — 직접 테스트 가능한 프로토타입 준비
- 현재: `2.5 사용자 전체 검토`
- 다음: `2.6 피드백 일괄 반영 및 2단계 확정`
- 공개 미리보기: `https://sandleapt.github.io/archive-v1/`
- 관리 작업대: `https://sandleapt.github.io/archive-v1/admin/`
- 운영 루트 `index.html` 변경: 없음
- 검색 A/B 최종 선택: 보류

## 작업 원칙

- 사용자가 큰 단계 전체 진행을 승인하면 소단계마다 컨펌을 요구하지 않고 다음 사용자 검토 지점까지 이어서 구현한다.
- `done`은 코드가 생긴 시점이 아니라 사용자가 확인한 뒤 사용한다.
- 사용자가 실제로 눌러볼 수 없는 상태를 `구현 완료`라고 과장하지 않는다.
- 보안·권한·데이터 모델 변경, 원본 훼손 가능성처럼 되돌리기 어려운 결정만 중간 확인한다.
- 큰 단계가 바뀌기 전 완료 / 남은 것 / 보류 결정 / 다음 단계 세부작업을 사용자에게 보여준다.
- 전체 10년치 일괄 변환 금지, 본 이관은 작은 배치로 진행한다.
- GitHub에는 코드·메타데이터·검색 텍스트·관계 인덱스 중심, 대용량 원본은 외부 저장소 중심.
- 입력은 한 번, 노출은 여러 곳.
- 공개등급은 `public / resident / private`.
- UI 구성과 데이터 구조를 분리한다.

## 현재 체크포인트 — 2.5 전체 검토

### 이번 보완 이유

- 사용자가 2.2 / 2.3 / 2.4를 각각 만들어진 상태로 직접 테스트할 것을 기대했으나, 기존 관리 작업대는 번호별 기능이 명확하게 드러나지 않아 `테스트할 만한 게 없다`고 판단함.
- 특히 2.3 공개등급은 독립된 테스트 화면이 없었으므로 기존 `2.1~2.4 done` 표기는 부정확했음.
- 이를 바로잡아 2.1~2.4 상태를 모두 `review`로 되돌리고, 번호별 직접 테스트 진입과 2.3 전용 화면을 추가함.

### 지금 직접 테스트 가능한 것

- `2.2 새 자료 등록`
  - `예시 채우기`
  - `AI 초안 만들기 · 시뮬레이션`
  - 생성된 자료가 실제 분류 검토함으로 이동
- `2.3 공개 등급`
  - 샘플 자료별 public / resident / private 직접 변경
  - 공개 빌드 포함/제외 결과 즉시 확인
- `2.4 분류 검토`
  - 주제 / 조직 / 현행상태 수정
  - 승인 / 보류 / 다시 검토
- `2.4 관계 검토`
  - 연결 대상 / 관계종류 / explicit·verified·inferred 수정
  - 승인 / 연결 없이 진행
- `발행 대기`
  - 공개등급 최종 확인
  - 미리보기
  - 브라우저 메모리 안에서 발행 처리

### 전체 테스트 순서

`2.2 예시 채우기 → AI 초안 만들기 → 2.4 분류 승인 → 2.4 관계 승인 또는 생략 → 발행 대기 → 공개등급 확인 → 미리보기 발행`

2.3은 위 흐름 중 언제든 별도 메뉴에서 공개등급을 바꿔볼 수 있다.

### 의도적으로 아직 연결하지 않은 것

- 실제 관리자 인증
- GitHub/Google Drive 쓰기 API
- resident 인증
- 실제 AI 호출
- 실제 공개 검색 인덱스 갱신
- 실제 원본 업로드

이 항목들은 3~4단계의 회의 연결·저장소·권한 구조와 함께 설계한다.

## 최근 인계

### 2026-09-01 14:02:45 KST — GPT — 2단계 테스트 가능 상태 보강

- 사용자가 기존 2단계 화면에서 2.2·2.3·2.4를 실제로 시험할 수 없다고 지적함.
- 기존 `2.1~2.4 done` 표기를 철회하고 `review`로 수정함.
- 관리자 왼쪽 메뉴에 번호를 직접 표시함: `2.2 새 자료 등록 / 2.3 공개 등급 / 2.4 분류 검토 / 2.4 관계 검토`.
- `archive-v1/admin/views/visibility.js`를 새로 추가해 2.3을 독립적으로 테스트할 수 있게 함.
- 관리자 테스트 홈에 각 단계별 `직접 테스트` 버튼과 전체 end-to-end 테스트 순서를 추가함.
- admin HTML의 CSS/JS 참조에 버전 쿼리를 붙여 이전 캐시로 오래된 화면이 남을 가능성을 줄임.
- 운영 루트와 Claude 예약 9건은 수정하지 않음.
- 관련 커밋: `fc92078a8eff45e0c3fb81bc9ddad8d6d0deb418`, `b29b3117757d87b6e1a0d523a7f31509107de4fe`, `e750af94d614219c9bcbc045202e0e611e4f6dcb`, `7c8b316488bd4bf164ef64923000731062aaa685`, `a036918eb1fa05fd1342899b9d3ee1b00fc808ef`.

### 2026-09-01 13:53:31 KST — GPT — 2단계 전체 프로토타입 작업

- 관리자 작업대를 좌측 메뉴형 작업공간으로 확장하고 등록·분류·관계·발행 흐름 파일을 추가함.
- 이후 사용자 검토에서 번호별 테스트성이 부족한 것으로 확인되어 14:02 보완함.

## 기록 규칙

작업 종료 시 현재 번호, 완료 번호, 다음 번호, 변경 파일, 주의사항, 완료 시각 `YYYY-MM-DD HH:mm:ss KST`, 관련 커밋을 남긴다.
