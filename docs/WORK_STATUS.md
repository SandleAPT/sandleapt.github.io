# 공동 작업 현황

마지막 갱신: 2026-09-04 15:38:26 KST

> GPT와 Claude가 번갈아 작업할 때 가장 먼저 확인하는 파일이다. 과거 상세 로그는 Git 커밋 이력에서 확인할 수 있다.

## 활성 작업

### FEES-20260904-04 — 관리비 2026년 6월분 원문 전체 재적재

- 담당: Claude
- 상태: `done` — 2026-09-04 15:38:26 KST
- 수정 범위: `fees/detail-2026.json`(6월 20개 섹션 추가, 음식물 1,142세대 범위 추가), `fees/data-2026.json`(6월 관리외 수익·예금 잔액 표 추가, 6월 출처 상태 full-document-verified), `fees/data.json`, `fees/reviews.json`(원문 단가 불일치 2건), `fees/README.md`, `fees/tools/check-fees.ps1`(관리비 증감 항등식 수정, 산출내역 없는 달 처리), `docs/WORK_STATUS.md`
- 변경: 6월분 원문 5~15쪽을 7월과 같은 구조로 옮김. 기존 6월 부과총괄·관리외 비용은 원문과 대조해 그대로 둠. 6월 게시일 2026-07-24, 납부기한 07-31
- 원문 내부 불일치: 일반관리비 단가 350.55 vs 350.50, 수선유지비 98.9/76.75 vs 98.86/76.63 — reviews.reconciliations 기록
- 검증: check-fees 6월(전월 5월)·7월(전월 6월) 전부 통과. 7월 관리외 수익 전월잔액이 6월 당월잔액과 연결됨을 스크립트가 확인. 로컬 화면에서 6월 블록 표시·월별 흐름 "관리외 수익 · 주차수입" 6→7월 비교 확인
- 남은 일: 5월부터 거꾸로 1~5월 재적재 → 6·7월 의결 대조 → 감사 사례집

### FEES-20260904-03 — 월별 흐름 항목에 관리외 수익 추가

- 담당: Claude
- 상태: `done` — 2026-09-04 15:28:22 KST
- 수정 범위: `fees/index.html`(CACHE_V 47), 포털 `index.html`(v359, FEES v=48), `docs/WORK_STATUS.md`
- 사용자 지적: "관리외비용 · 소주제 식 표시가 알아보기 쉬운데 관리외수입은 그 표시가 없어 불편" — 월별 흐름 탭 항목 선택에 관리외 비용만 있었음
- 변경: 항목 선택에 "관리외 수익 · 항목" 18개 추가(코드 `inc:이름`), 월별 값은 당월발생액, 원문 비고 표시. 검증: 문법 통과, 옵션 63개 중 수익 18개, "관리외 수익 · 주차수입" 선택 시 2026-07 8,791,450원과 비고 표시
- 남은 일: 1~6월은 관리외 수익 표가 아직 없어 전월 비교가 "자료 없음" — 재적재하면 채워짐

### FEES-20260904-02 — 관리비 화면에 원문 5~15쪽 표 표시(관리외 수익·예금 잔액·항목별 산출내역)

- 담당: Claude
- 상태: `done` — 2026-09-04 02:30:18 KST
- 수정 범위: `fees/index.html`(CACHE_V 45→46), 포털 `index.html`(verNo v357→v358, FEES 캐시 키 v=46→47; MINQ·archive-v1 키는 회의록·Archive 변경이 없어 357 유지), `docs/WORK_STATUS.md`
- 사용자 지적: "관리외수익은 없어" — 데이터에는 넣었지만 화면에 없던 것. 이번 묶음에서 표시
- 변경: 전체 상세내역 탭에 (1) 원문 14쪽 관리외 수익(묶음별 소계·합계) (2) 15쪽 예금 잔액 현황(계좌번호 제외) (3) 표지의 산출기간·납부기한 (4) 5~13쪽 항목별 산출내역 20개 섹션을 접이식 블록으로 추가. 표 종류(allocation·breakdown·split·composition·usage·elevatorByBuilding·insurance·parking)별 렌더러. detail-YYYY.json은 data.json의 detailFile로 읽고, 없는 달은 "아직 옮기지 않음" 안내 블록만 표시. 검색창이 새 블록의 항목명·비고·내역까지 걸러냄
- 실수·교훈: loadData 한 줄짜리 코드 중간에 `//` 주석을 넣어 뒤쪽이 통째로 주석 처리돼 SyntaxError — 한 줄 코드에는 `/* */`만 쓸 것. 브라우저에서 `new Function(스크립트 전체)`로 문법 재확인
- 검증: 로컬 서버(scratchpad serve-site.ps1, 8931)에서 7월 열어 콘솔 오류 없음, 블록 31개 제목 확인, 수선유지비 블록 표 3개(내역 34행·부과 7행×2), 검색 "스프링클러"로 해당 블록만 열림, 6월 선택 시 안내 블록 표시
- 남은 일: 7월 의결 대조 → 1~6월 재적재(6월부터) → 감사 사례집

### FEES-20260904-01 — 관리비 2026년 7월분 원문 전체 적재(항목별 산출내역 포함)

- 담당: Claude
- 상태: `done` — 2026-09-04 02:18:51 KST
- 수정 범위: `fees/data-2026.json`, `fees/detail-2026.json`(신설), `fees/data.json`, `fees/reviews.json`, `fees/README.md`, `fees/tools/check-fees.ps1`(신설), `docs/WORK_STATUS.md`
- 사용자 지시: "관리비내역서는 실제 내용이 엄청 많은데, 축약되거나 누락된거 다시 꼼꼼하게 적재 및 보기 편하게 정리." 원본 PDF(2026년 1~7월, 네이버카페 인쇄본)는 `Downloads`에 있음
- 변경: 7월분 원문 4~15쪽 전체를 옮김. 부과총괄 28행·그룹합계 5행은 data-2026.json, 항목별 산출내역 20개 섹션(계약업체·계약기간·산출근거·면적별 부과·수선 건별 31행·동별 승강기 전기료 37행 등)은 새 파일 detail-2026.json, 관리외 수익(신규 표)·관리외 비용·예금 잔액(신규 표)은 supplementalTables. 6월 출처에 PDF 파일명·SHA-256 보강
- 원문 내부 불일치 2건은 reviews.json reconciliations에 기록(수선유지비 인쇄 단가 127.65/97.95 vs 나눗셈 127.62/97.85, 음식물 면제세대 29 vs 12+16=28)
- 검증: 6개 JSON 파싱, 부과총괄 행합계=당월부과액·그룹합계·행별 항등식·6월 연결, 관리외 수익/비용 잔액 연속성·6월 연결, 예금 합계, 산출내역 20섹션의 표별 합계·세대수×단가·부과차액·부과총괄 대조, 섹션 금액 합=당월부과액 — `fees/tools/check-fees.ps1` 전부 통과(경고 2건은 위 원문 불일치)
- 화면: index.html은 아직 손대지 않음(새 표 종류는 무시되고 7월 부과총괄·관리외 비용만 표시됨). 다음 묶음에서 산출내역·관리외 수익·예금잔액 표시와 CACHE_V 갱신
- 남은 일: (1) 7월 의결 대조(decisions.json reviewedThrough 2026-06→07) (2) 화면에 산출내역 표시 (3) 1~6월 산출내역·관리외 수익·예금잔액을 6월부터 거꾸로 월 단위 적재 (4) 경기도 감사 사례집 2018~2025 정리(파일은 Downloads에 있음)

### MINUTES-20260903-12 — 계약·기준문서 열람 비밀번호 잠금

- 담당: GPT
- 상태: `done` — 2026-09-03 17:23:59 KST 잠금 해제
- 수정 범위: minutes `assets/js/app/notices.js`, `index.html`, `CHANGELOG.md`; portal `index.html`, `docs/WORK_STATUS.md`
- 사용자 결정: 계약·기준문서도 기존 열람용 비밀번호로 잠금
- 변경: 계약 탭과 포털 메뉴에 🔒 표시, 열람용 또는 수정용 비밀번호의 서버 검증을 통과하기 전에는 계약 화면과 `contracts.json`을 앱에서 불러오지 않음
- 보존: 관리규약·선거/선관위 공개 유지, 계약 데이터·검색·카드 UI·기존 해시 유지
- 보안 한계: `contracts.json`은 공개 GitHub Pages의 정적 파일이므로 직접 주소 접근까지 막는 보안 저장은 아님. 완전 비공개가 필요하면 별도 비공개 저장소로 이전 필요
- 검증: notices.js 및 전체 인라인 스크립트 문법, 잠금 분기 순서, 잠금 전 loadContracts 미호출, 전체 캐시 키 v340, 포털 iframe v340·포털 v345, `git diff --check` 확인
- 배포: 이 기록과 함께 minutes v340·portal v345로 반영
- 남은 일: 없음

### PORTAL-20260903-11 — 네비게이션 선택 색상 통일

- 담당: GPT
- 상태: `done` — 2026-09-03 11:24:35 KST 잠금 해제
- 수정 범위: 포털 `index.html`, `docs/WORK_STATUS.md`만
- 사용자 결정: 대분류별 초록·파랑 의미색을 없애고 모든 메뉴를 같은 중립색으로 표시하며, 현재 선택된 항목만 포털 기본 노란색으로 강조
- 변경: PC 왼쪽 메뉴·전 화면 상단 대/중/소분류·모바일 하단 메뉴의 선택 색상 규칙을 하나로 통일
- 보존: 메뉴 구조·이동 동작·본문·데이터·기존 해시를 변경하지 않음
- 검증: 인라인 스크립트 문법, 초록·파랑 전용 클래스 및 변수 제거, 포털 v344 표기, `git diff --check` 확인
- 배포: 이 기록과 함께 포털 v344로 반영
- 남은 일: 없음

### PORTAL-20260903-10 — 전 화면 계층 탐색 및 모바일 대분류 바로가기

- 담당: GPT
- 상태: `done` — 2026-09-03 11:12:54 KST 잠금 해제
- 수정 범위: 포털 `index.html`, `README.md`, `docs/WORK_STATUS.md`만
- 변경: 모바일 하단의 5개 대분류를 누르면 서랍을 여는 대신 해당 분류의 첫 번째 실제 화면으로 바로 이동
- 상단 탐색: 모든 화면에 대분류 선택 줄과 현재 대분류의 중분류 선택 줄을 표시하고, `회의 관리` 화면에서만 안건·발언 / 회의 설정 / 동대표 명단 소분류 줄을 추가 표시
- 현재 위치: 선택된 대·중·소분류 버튼에 활성 상태와 `aria-current`를 적용
- 보존: 기존 본문·데이터·본문 내부 탭·검색·Archive 데이터·기존 메뉴 ID와 해시를 변경하지 않음
- 검증: 인라인 스크립트 문법, 대분류별 첫 화면 매핑, 회의 관리 3단계 생성, 기존 해시 목록, `git diff --check` 확인
- 배포: 이 기록과 함께 포털 v343으로 반영
- 남은 일: 없음

### PORTAL-20260903-09 — 모바일 대분류 네비게이션 통일

- 담당: GPT
- 상태: `done` — 2026-09-03 11:05:35 KST 잠금 해제
- 사용자 확인: v341은 서랍 안만 재분류되고 모바일 하단 바로가기와 상단 표시가 이전 구조 그대로여서 개편 결과가 보이지 않았음
- 수정 범위: 포털 `index.html`, `README.md`, `docs/WORK_STATUS.md`만
- 변경: 모바일 하단을 회의 / 검색·아카이브 / 기준·문서 / 점검·관리 / 소식·정보로 교체하고, 누르면 해당 대분류 위치로 서랍을 열도록 연결
- 상단 표시: 현재 메뉴명만 표시하던 것을 `대분류 › 현재 메뉴` 경로로 변경
- 보존: 본문 화면·데이터·내부 탭·검색·Archive·기존 해시를 변경하지 않음
- 검증: 공개 v342에서 하단 5개 대분류 문구, 상단 `기준 · 문서 › 선거 · 선관위`, PC 좌측 5개 대분류를 확인; 인라인 스크립트 문법과 `git diff --check` 통과
- 배포 커밋: `9f7911a103a277425fbd4538f449534fe19f35f1`
- 남은 일: 없음

### PORTAL-20260903-08 — 회의·검색 영역 네비게이션 분리

- 담당: GPT
- 상태: `done` — 2026-09-03 10:40:39 KST 잠금 해제
- 수정 범위: 포털 `index.html`, `README.md`, `docs/WORK_STATUS.md`만
- 변경: 회의에서 Archive 시험판·주제별 보기를 빼 `검색 · 아카이브`로 이동하고, 회의 관리 3개 메뉴는 중간 제목과 들여쓰기로 계층만 표시
- 준비 자리: `통합검색 (준비 중)`을 비활성 메뉴로 표시하며 새 화면·검색 기능·해시는 만들지 않음
- 보존: 기존 본문·데이터·내부 탭 UI·검색 기능·Archive 데이터·기존 메뉴 ID와 해시를 변경하지 않음
- 검증 완료: 인라인 스크립트 문법, 5개 대분류 순서, 기존 해시 ID, 준비 항목 비활성화, `git diff --check`
- 공개 확인: v341, 5개 대분류 순서, 회의 관리 계층, 비활성 통합검색 자리 모두 배포본에서 확인
- 배포 커밋: 포털 `52925de149719426ba28f052465d9915787e5cd7`, README `5dcbcef5dad17ad563d9816065e43f7b36481d57`
- 남은 일: 없음. 통합검색 기능과 모바일 신규 UI는 이번 작업 범위 밖으로 유지

### PORTAL-20260903-07 — 좌측 메뉴 목적별 재분류

- 담당: GPT
- 상태: `done` — 2026-09-03 10:18:43 KST 잠금 해제
- 수정 범위: 포털 `index.html`, `README.md`, `docs/WORK_STATUS.md`만
- 변경: 회의 / 기준 · 문서 / 점검 · 관리 / 소식 · 정보의 4그룹으로 좌측 메뉴를 재분류
- 연결: 기존 `noticeView`를 그대로 재사용하면서 관리규약·계약·선거·공고·절차 점검의 기존 내부 탭을 직접 선택
- 보존: 기존 본문·데이터·카드·탭 UI·회의 세부 메뉴·기존 해시 ID는 변경하지 않음
- 검증 완료: 인라인 스크립트 문법, 필수 메뉴·하위 탭 매핑, 기존 해시 ID, `git diff --check`; 공개 v340 데스크톱에서 5개 메뉴를 차례로 클릭해 왼쪽 활성 항목·본문 내부 탭·해시가 일치함을 확인
- 모바일 확인: 기존 860px 반응형 서랍·세로 스크롤·클릭 후 닫기 로직은 변경하지 않았고, 새 메뉴명은 기존 최장 항목보다 짧아 폭 조건에 영향 없음
- 배포 커밋: 포털 `2fa77c07cbc145d64aa4e2baa3656992a495d47b`, README `1d16592abe10a56628a31f31c393a25ab525c112`
- 남은 일: 없음. 회의록 세부 메뉴 접기·재구성은 사용자 지시대로 별도 작업으로 보류

### MINUTES-20260903-06 — 절차 점검 14건 자동 복구

- 담당: GPT
- 상태: `done` — 2026-09-03 01:43:21 KST 잠금 해제
- 증상: 절차 점검이 14건에서 5건으로 줄고 ② 묶음이 표시되지 않음
- 원인: 클라우드 `checks_v1` 응답 실패로 정적 `investigations.json` 5건만 렌더링됨
- 조치: 클라우드 우선, 실패·4초 지연 시 `system-backup.json`의 `checks_v1` 9건을 자동 사용
- 표시 결과: ① 7건 · ② 5건 · ③ 2건, 전체 14건 복구
- 버전: minutes·portal 통합 v339

### PORTAL-20260903-05 — 포털 빈 화면 긴급 복구

- 담당: GPT
- 상태: `done` — 2026-09-03 01:33:04 KST 잠금 해제
- 원인: 포털 배포 시 `index.html`의 앞 260줄만 전송되어, 메뉴와 첫 화면을 만드는 마지막 스크립트가 누락됨
- 조치: 393줄 전체 파일을 다시 배포하고 포털 버전을 v338로 갱신
- 보존 범위: minutes v136 절차 점검 구조와 데이터는 변경하지 않음
- 검증: 전체 HTML 닫힘 태그·인라인 스크립트 문법·공개 화면의 메뉴 및 iframe 생성 여부 확인

### MINUTES-20260903-04 — 계약·기준문서형 절차 점검 재구성

- 담당: GPT
- 상태: `done` — 2026-09-03 01:21:52 KST 잠금 해제
- 사용자 방향: 계약·기준문서처럼 닫힌 목록에서도 핵심이 보이고, 1차로 펼치면 요약, 더 펼치면 사실·규약·자료가 종류별로 나뉘는 계층 사용
- 작업 범위: v135의 전체 펼침 사건 보드를 계약·기준문서와 같은 점진적 공개 구조로 교체
- 보존 범위: 점검 데이터·상태·문제 묶음·관련 기록·잠금 방식은 변경하지 않음
- 표시 결과: 닫힌 카드에 상태·제목·핵심 한 줄, 1차 펼침에 요약·확인할 것·다음 조치, 2차 펼침에 확인된 사실·적용 기준·확인자료·관련 기록을 각각 배치. 위반·미충족 확인 항목은 맨 위 「★ 먼저 볼 점검」에 별도 집계
- 검증: `notices.js` 문법 검사, 실제 조사 5건+규약대조 9건 모의 로드로 닫힌 카드 14개·핵심 줄 14개·분류별 상세·중임 카드 노출 확인, 캐시 키 대조, `git diff --check` 통과
- minutes 커밋: `90bbd41791d391bf10f93c33ce3f9240f76926c9`

### MINUTES-20260903-03 — 절차 점검 읽기 화면 리뉴얼

- 담당: GPT
- 상태: `done` — 2026-09-03 01:10:07 KST 잠금 해제
- 작업 범위: 절차 점검의 3중 접기 구조를 1단계로 줄이고, 상태·핵심 판단·확인할 것·다음 조치를 먼저 읽을 수 있는 사건 보드형 화면으로 개편
- 보존 범위: 점검 데이터·상태·근거·관련 기록·비밀번호 잠금은 변경하지 않음
- 표시 결과: 문제 묶음과 카드 본문은 항상 표시, 문제 흐름은 번호 단계, 긴 사실·규약·증빙·관련 기록만 「근거 자료 펼쳐보기」로 접음. 모바일은 흐름 세로화·상태 필터 가로 스크롤
- 검증: `notices.js` 문법 검사, 실제 조사 5건+규약대조 9건을 모의 로드하여 묶음 3개·카드 14개·근거 접기 14개·핵심 구역 렌더 확인, 캐시 키 대조, `git diff --check` 통과
- minutes 커밋: `80a2f335ee31241568fd8bd6078fb01c0fa9e9e1`

### MINUTES-20260903-02 — 임차 6기 중임 점검을 잠금 화면으로 이동

- 담당: GPT
- 상태: `done` — 2026-09-03 00:53:00 KST 잠금 해제
- 작업 범위: 공개 `선거·선관위`의 중임 제한 대상 표시를 제거하고, 비밀번호가 필요한 `절차 점검`의 기존 `c_term_limit` 항목으로 통합
- 주의: 공개 임대세대 분포 자체는 유지하되 대상자·참고 최소 찬성표는 공개 데이터에서 제거
- 표시 결과: `절차 점검 🔒`의 「기수를 가로지르는 반복 패턴」 안에 「임차 6기 중임 제한 대상 — 반드시 확인할 것」 카드로 표시
- 검증: `notices.js` 문법 검사, `elections.json` 파싱, 공개 렌더러·공개 JSON에서 점검 블록 제거, 버전·캐시 키 대조, `git diff --check` 통과
- minutes 커밋: `9ed9b52024c8331d60905346429b13da1d4ef838`

### MINUTES-20260903-01 — 임차 6기 중임 예외 찬성요건 점검 표시

- 담당: GPT
- 상태: `done` — 2026-09-03 00:38:21 KST 잠금 해제
- 작업 범위: `SandleAPT/minutes`의 선거·선관위 화면에서 임대세대 분포와 임차 6기 중임 제한 대상자의 찬성요건을 연결해 표시
- 변경 파일: minutes `elections.json`, `assets/js/app/notices.js`, `index.html`, `docs/DATA.md`, `CHANGELOG.md`; 포털 `index.html`, `docs/WORK_STATUS.md`
- 기준: 임대주택 관리규약(2020.04.18) 제16조제4항. 2회 공고 무후보·비중임 후보 부재를 전제로 해당 선거구 전체 임차인 2분의 1 이상 찬성 필요
- 주의: 2026.08.19 임대세대 분포는 2025.12.24 선거 뒤 자료이므로 최소 찬성표는 참고값으로만 표시하고, 확정 판단은 선거 당시 선거인명부와 찬성표로 대조
- 표시 결과: 진세택(제2선거구·203동) 현재 분포 참고 16표 이상, 원영해(제5선거구·210동) 14표 이상, 강명순(제9선거구·216동) 14표 이상. 모두 `선거 당시 수치 확인 필요`로 표시
- 검증: `notices.js` 문법 검사, `elections.json` 파싱·대상 3명·참고 최소 찬성표 계산, `git diff --check` 통과
- minutes 커밋: `d4f6d1373b180582064f6d347a2519d048451489`
- 남은 일: 관리주체 또는 선관위에서 2025.12.24 선거 당시 선거인명부와 대상자별 찬성표를 받으면 실제 충족 여부 갱신
- 다음 작업자가 할 일: 당시 원본 수치를 확보하기 전에는 참고 최소 찬성표를 확정 수치로 바꾸거나 당선 무효·규약 위반으로 단정하지 않는다.

### MINUTES-20260901-01 — 입대의 데이터 대기 9건 — `done` (잠금 해제)

- 담당: Claude
- 상태: `done` — 2026-09-01 16:50 KST 잠금 해제
- 결과: 입대의 게시판 1~9페이지 전수 이관 완료. 회의록 클라우드 236건(2016.05~2026.08), 공고 보관함 56건.
- 함께 처리: 주제 흐름 요약 38종 전면 갱신(stale 0), 동대표 임기 회차 계산 버그 수정(minutes v99~v101), 입대의 1~3기 좌석 확정.
- 원본 저장소 `SandleAPT/minutes`에서만 작업했고 이 저장소(포털·Archive)에는 iframe 캐시 키 외 변경 없음.
- **다른 작업자는 이제 입대의 데이터를 참조·분류해도 된다.** Archive 3단계 live-data 검증이 쓰는 `/minutes/data-YYYY.json`이 이 작업으로 2016~2019 샤드까지 늘어났다(아래 위험 항목 참조).

### ARCHIVE-20260901-01 — Sandle Archive v1

- 담당: **Claude** (2026-09-01 17:06 KST 사용자 지시로 GPT → Claude 이관)
- 상태: `in_progress` — 4.1·4.2·4.3a(GPT), 4.3b·4.3d·4.3e·4.3g(Claude) 완료. 남은 것: 4.3c·4.3f·4.4·4.5·4.6
- 마스터 계획: `docs/archive-v1/ROADMAP_V1.md`
- 1단계: `done` — 임시 확정
- 2단계: `review` — 직접 테스트 가능, 실사용 중 보완
- 3단계: `done` — 검증·인계 체크포인트 배포 완료
- 완료: `4.1 저장소 역할 분리`, `4.2 SourceReference v1`, `4.3a 공개 발행 경계`
- 현재: `4.3b 실제 입주민·관리자 인증 결정 대기`
- 마지막 검증 완료: 실제 회의 213건 / 안건 1,125건 전체 변환
- 다음: 공개된 4단계 정책 화면 검토 후 인증 방식은 사용자와 결정
- 공개 미리보기: `https://sandleapt.github.io/archive-v1/`
- 3단계 직접 보기: `https://sandleapt.github.io/archive-v1/admin/#meetingImport`
- 4단계 직접 보기: `https://sandleapt.github.io/archive-v1/admin/#storagePolicy`
- 운영 루트 `index.html` 변경: 없음
- `SandleAPT/minutes` 원본 코드/데이터 변경: 없음
- 검색 A/B 최종 선택: **B안 확정**(2026-09-02 사용자 결정 — 주제 이름을 눌러도 같은 화면)
- 현재 진행: `4.3d 브라우저 검증 러너` — 아래 '검증 환경 제약' 참조

#### 검증 환경 제약 (2026-09-01 17:06 KST, Claude 확인)

인계된 검증 명령이 이 작업 환경에서는 **실행 불가**하다.

- 사용자 PC(Windows)에 **Node.js가 설치돼 있지 않다.** `node -v` 실패.
- `archive-v1/tests/*.test.js`는 `require('node:assert/strict')`, `fs`, `path`, `vm`을 쓰는 CommonJS라 브라우저에서 그대로 못 돌린다.
- 따라서 담당이 Claude인 동안에는 기존 명령으로 `done` 판정을 할 수 없다. VALIDATION_POLICY 3항(자동 검증)을 충족하려면 node 없이도 같은 단언을 돌릴 경로가 필요하다.

임시 확인은 마쳤다. 브라우저 콘솔에서 `stage4-visibility` 단언 7개를 수동 재현해 전부 통과(2026-09-01 17:09 KST, `/archive-v1/admin/#storagePolicy`).
다만 수동 재현은 재현성이 없어 검증 기록으로 삼지 않는다.

**대응 방침(4.3d):** 기존 node 테스트 파일은 **수정하지 않는다**(node 없는 환경에서 회귀를 확인할 수 없으므로 건드리는 것 자체가 위험). 대신 케이스를 UMD spec 모듈로 새로 분리하고 브라우저 러너를 추가한다. node가 있는 환경(GPT)에서 기존 테스트를 같은 spec 기반으로 통합하는 것은 별도 소번호로 남긴다.

#### 외부 저장소 (2026-09-01 21:10 KST 확정)

`resident`/`private` 원본 파일은 **사용자 개인 구글 계정의 드라이브**에 둔다. 공개 정적 번들에는 절대 넣지 않는다(`AUTH_V1.md` 5절).

- 용량 제약 없음(5TB 중 약 156GB 사용, 직접 확인).
- 이 계정에서 회의록 GAS와 ⑨ 비공개 자료 GAS가 이미 돌고 있어 관리 지점이 늘지 않는다.
- **계정 주소와 폴더 id는 이 저장소에 적지 않는다.** 이 저장소는 공개돼 있어 검색에 노출된다. 실제 값은 사용자 PC의 비공개 메모(`산들마을-비공개\Archive 외부 저장소 계정.md`)에 있다.

**AI는 로컬 파일을 드라이브에 업로드할 수 없다.** 도구 정책으로 차단된다(2026-09-01 확인). 폴더 생성·구조 확인까지는 되지만 파일 전송은 사용자가 직접 해야 한다. 다음 작업자는 업로드를 대신하겠다고 약속하지 말 것.

아직 미착수: Drive에 파일로 두고 `file_id`만 참조하는 방식(현재 ⑨ 기능은 시트 셀에 dataURL로 넣어 큰 파일에 부적합), 폴더 규칙, Apps Script의 비밀번호 확인 후 전달 경로. 링크 공개 전환은 인증 체계를 무력화하므로 **사용자 승인 없이 하지 않는다.**

### 아직 사용자에게 물어봐야 하는 것

- (없음 — 저장 위치는 2026-09-02 02:10 결정됨. 「사용자 결정 기록」 참조)

### 나중에 할 일 — 회의 레코드를 안건 단위로 쪼개기

사용자 제안(2026-09-02). **지금 하지 않는다.** 설계 근거·제약은 `SandleAPT/minutes` `docs/SCALING.md` §5에 적어 뒀다.

요지: 지금은 안건 태그 하나를 바꾸려 해도 회의 전체(8~12K자)를 다시 저장한다. 이 구조가 2026-09-01 날짜 유실 64건과 2026-09-02 저장 전 형식 차이 확인의 공통 원인이다. 쪼개면 안건 단위 수정이 그 안건만 건드리고 Archive의 Fragment 개념과도 맞는다.

다만 레코드가 224 → 1,400건 이상으로 늘어 GAS 순차 조회가 더 느려지므로 **증분 조회(`action=list&since=`)가 먼저** 필요하고, 회의록 앱·정적 사본·백업·Stage 3 변환이 모두 “회의 하나 = 레코드 하나”를 전제하고 있어 되돌리기 어렵다. 그래서 별도 설계 단계로 다룬다.

## 사용자 결정 기록

되돌리기 어려운 항목은 AI가 임의로 정하지 않고 사용자에게 물어본다(`VALIDATION_POLICY.md` "사용자 확인이 필요한 예외"). **물어봐서 받은 답은 여기에 시간순으로 남긴다.** 대화창에만 있으면 다음 작업자가 같은 것을 다시 묻거나, 근거 없이 다르게 구현하게 된다.

| 시각(KST) | 항목 | 결정 | 반영 위치 |
|---|---|---|---|
| 2026-09-01 19:45 | **4.3b 입주민·관리자 인증 방식** | 별도 회원제를 만들지 않고 **회의록 앱의 열람용/수정용 비밀번호를 그대로 재사용**한다. `resident` = 열람용 이상, `private` = 수정용만. | `docs/archive-v1/AUTH_V1.md` |
| 2026-09-01 16:30 | 입대의 1~3기 명단 보강 범위 | 동호수가 확인되는 사람만 좌석에 넣고, 근거 없는 인물은 추정하지 않는다("추정 가능한 사람만"). 이후 관리사무소 구성현황 문서로 대부분 확정됨. | `SandleAPT/minutes` `scripts/import/roster_fix_2026-09-01.js` |
| 2026-09-01 17:06 | ARCHIVE 담당 이관 | Archive v1 작업 담당을 GPT → Claude로 옮긴다. | 이 문서 활성 작업 |
| 2026-09-02 09:10 | **검색 화면 A/B 최종 선택** | **B안으로 확정.** 근거(사용자): “검색해서 들어가서 보이는 화면이 더 마음에 들어.” 첫 화면에서 주제 이름을 눌러도 같은 화면이 나오게 통일. A안과 주제 상세 화면은 코드에 남겨 둔다(전환 버튼으로 볼 수 있고, `search-b.js`가 빠져도 화면이 비지 않는다). **샘플로는 판단이 어려웠고 실제 자료를 붙인 뒤에야 정할 수 있었다.** | `ROADMAP_V1.md` 1.10, `archive-v1/search-b.js` |
| 2026-09-02 02:10 | **주제 훑어보기에서 고친 값의 저장 위치** | **회의록 앱의 안건 태그(`a.tags`)에 직접 쓴다.** 근거(사용자): “태그는 결국 양쪽 다 쓰는 것”. Archive가 별도 분류 저장소를 두지 않고 회의록 태그를 단일 출처로 삼는다. → 「원본 덮어쓰기」에 해당하지만 사용자가 명시적으로 결정함. **쓰기 구현 전에 안전장치 필요**(아래 인계 참조). | `admin/views/topic-review.js` |
| 2026-09-01 23:20 | **`2.4` 분류·관계를 자동 판정으로** | 하나씩 승인받지 말고 **자동으로 정하고, 사용자가 이상하다고 느낀 것만 수동으로 고친다.** 회의 224건·안건 1,125건을 일일이 승인하는 것은 불가능하다. 자동 확정된 항목은 목록에서 꺼내 되돌릴 수 있어야 한다. | `shared/auto-assign.js`, 분류 검토 화면 |
| 2026-09-01 23:20 | **`4.3` 공개 등급 3단계와 연결 노출** | 등급은 **공개 / 내부공개 / 비공개**. 비로그인은 공개만, 열람용은 내부공개까지, 수정용은 전부. **볼 수 없는 등급의 기록과의 연결은 아예 보이지 않아야 한다**(가리기만 하면 제목이 샌다). 발행 자체는 수동으로 막지 않는다 — 통제는 등급으로 한다. | `shared/access-control.js` `filterRelations`/`projectRecord` |
| 2026-09-01 21:10 | **`4.1` resident/private 원본 외부 저장소** | 새 서비스를 만들지 않고 **사용자 개인 구글 계정의 드라이브**를 쓴다. 저장 위치는 `산들마을 보안문서` 아래 하위 폴더. 계정 주소·폴더 id는 개인정보라 **공개 저장소에 적지 않는다**(사용자 로컬 비공개 메모에 기록). | 이 표 + 아래 「외부 저장소」 |

### 아직 사용자에게 물어봐야 하는 것

- resident/private **원본 파일을 어디에 두고 어떻게 내려줄지**(외부 저장소 계정·비용과 연결) — `4.1`·`4.3b` 7절
- 공개 범위를 넓히는 변경 일반
- 운영 루트(`sandleapt.github.io`) 전환 시점 — `7.6`

## 작업 원칙

- 담당 AI가 각 단계의 기능·데이터·호환성을 직접 검증하고 보완한 뒤 사용자 중간 컨펌 없이 다음 단계로 진행한다.
- `done`은 코드가 생긴 시점이 아니라 자동 검증·실제 데이터 검증·인계 기록까지 끝난 뒤 사용한다.
- 사용자가 실제로 눌러볼 수 없는 상태를 `구현 완료`라고 과장하지 않는다.
- 사용자 피드백은 담당 AI가 관련 Roadmap 단계와 영향 범위를 판단해 새 소번호로 반영한다.
- 보안·권한·데이터 모델 변경, 원본 훼손 가능성처럼 되돌리기 어려운 결정만 중간 확인한다.
- 소단계 또는 검증 가능한 기능 묶음마다 `WORK_STATUS`·설계·검증 기록을 갱신하고 체크포인트 커밋을 남긴다.
- 다른 AI에게 이어달라고 할 때는 `docs/CONTINUE_ARCHIVE_PROMPT.md`의 프롬프트를 사용한다.
- 전체 10년치 일괄 변환 금지, 본 이관은 작은 배치로 진행한다.
- 입력은 한 번, 노출은 여러 곳.
- UI 구성과 데이터 구조를 분리한다.

## 현재 체크포인트 — 4.3a 공개 발행 경계

- 공개 포털, minutes, 외부 원본 저장소의 역할을 분리함.
- SourceReference v1과 public projection을 구현함.
- 공개등급 화면, 저장·권한 화면, 발행 대기 화면이 같은 정책 모듈을 사용함.
- resident/private는 화면과 `store.publish()` 양쪽에서 공개 발행을 차단함.
- 정책 모듈이 없을 때도 발행을 막는 fail-closed 방식임.
- 캐시 버전: `20260901-152900`
- 검증 문서: `docs/archive-v1/STAGE4_VALIDATION.md`

## 다음 AI가 바로 할 일

1. 최신 main에서 `AGENTS.md`, `docs/AI_WORKFLOW.md`, 이 문서, Roadmap과 Stage 4 검증 문서를 읽는다.
2. 검증을 다시 실행한다. **환경에 따라 경로가 다르다.**
   - node 있음: 기존 `node archive-v1/tests/*.test.js`
   - node 없음: 브라우저로 `/archive-v1/tests/browser/` 접속 → `전체 검증 실행` (**spec 7종 전부**, 실제 데이터 포함)
3. `4.3b` 인증 방식은 **결정·구현 완료**(비밀번호 재사용). 남은 인증 과제는 `4.3c`(시도 제한·감사 로그)와 resident/private 원본 전달 서버(저장소 결정 필요 — 사용자 확인 항목).
4. 안전한 범위의 다음 후보: `4.4` 로그인 UI·등급별 화면 연결, `4.6a` 데이터 신선도 표시, `4.3f` node 테스트 spec 통합.
5. `MINUTES-20260901-01`은 `done`으로 해제됨 — 입대의 데이터를 참조·분류해도 된다.

```bash
# node가 있는 환경
node archive-v1/tests/stage3-source.test.js
node archive-v1/tests/stage3-adapter.test.js
SANDLE_MINUTES_ROOT=/path/to/minutes node archive-v1/tests/stage3-live-data.test.js
for test in archive-v1/tests/stage4-*.test.js; do node "$test" || exit 1; done
```

```text
# node가 없는 환경 (Stage 4)
https://sandleapt.github.io/archive-v1/tests/browser/  →  [전체 검증 실행]
결과: window.__sandleTestResult = {at, rows, failed}
```

### 주의 — Archive가 보는 데이터는 클라우드보다 뒤처진다 (2026-09-01 실측)

Archive Stage 3은 `/minutes/data-YYYY.json` **정적 샤드**를 읽는다. 이 파일은 minutes 봇이 주기적으로 클라우드에서 다시 만들기 때문에 항상 한 주기 뒤처질 수 있다.

- 2026-09-01 19:20 KST 실측: 클라우드 회의록 **224건** / 정적 샤드 **213건**
- 샤드 생성 시각 `2026-09-01 07:18 KST`, 차이 11건은 그 이후 적재된 입대의 1기(`m_2016_*`·`m_2017_02·04·05·05s`·`m_2018_04`)
- **데이터 손실이 아니라 재발행 지연이다.** 다음 재발행 후 `stage3-live-data`가 224건으로 늘어난 채 통과하는 것이 정상이며, 건수 변화를 회귀로 오판하지 말 것.
- 후속 검토는 `4.6 Archive 데이터 신선도`로 등록됨.

## 이전 체크포인트 참고 — 3.7 검증 보완·인계

사용자 목표는 **현재 만들어진 회의록을 다시 작성하는 것이 아니라 더 보기 편하게 정리하는 것**으로 잡았다.

사용자는 단계마다 직접 확인하는 방식 대신 담당 AI가 스스로 검증·보완하며 진행하고, 나중에 결과를 본 뒤 수정 요청을 하면 관련 단계를 찾아 반영하는 방식으로 변경했다. 긴 작업이 중간에 끊겨도 다른 AI가 이어갈 수 있도록 기능 묶음마다 체크포인트를 먼저 남긴다.

### 이번 체크포인트에서 검증된 것

- fixture source 로드와 연도 캐시
- 회의 → Document / 안건 → Fragment 변환
- 표결, 의결, 후속조치, 분류 검토 Draft 생성
- 실제 공개 회의 213건 / 안건 1,125건 전체 변환
- JSON 파싱 오류 0건
- Fragment ID 중복 0건
- 기존 minutes 원본 변경 없음

### 검증 중 발견해 보완한 것

- 과거 분류명 `미화` → `청소·미화`
- 과거 분류명 `소송` → `하자·소송`
- `저수조·청소`, `기타`는 최신 taxonomy 키워드로 다시 후보 생성
- 자동 보완된 주제는 확정 저장 태그가 아닌 `inferred`로 표시

### 3단계에서 직접 테스트 가능한 것

`관리 도구 → 3. 회의록 → Archive`

- 실제 `/minutes/data-index.json` 및 `/minutes/data-YYYY.json` 읽기
- 연도 선택
- 입주자대표회의 / 임차인대표회의 필터
- 회의명 검색
- 실제 저장된 회의 선택
- 기존 회의 안건과 Archive 변환 결과 비교
- 회의 1건 → `Document`
- 안건별 → `Fragment`
- 논의 내용 / 의결사항 / 후속조치 분리
- 표결 찬성/반대/기권 요약
- 기존 저장 태그 우선 사용
- 태그가 없거나 `기타`인 경우 현재 minutes taxonomy로 주제 후보 표시
- 자동 주제는 확정 사실이 아니라 후보로 표시
- 안건 Fragment를 2.4 분류 검토함으로 전송
- 같은 안건 반복 전송 시 중복 방지

### 원본 보존 원칙

- 기존 minutes 회의 작성 UI 변경 없음
- 기존 cloud 저장 구조 변경 없음
- 기존 회의록 미리보기 / PDF / 1페이지 출력 변경 없음
- Archive는 기존 회의록을 읽기만 하고 검색·주제·타임라인에 맞게 재배치하는 역할
- 회의 작성 화면에 Archive용 필드를 추가해 이중 입력시키지 않음

### 2단계와 연결

- 일반 자료와 회의 안건이 같은 분류 검토함을 사용
- 회의록에서 가져온 항목은 `회의록 변환`으로 표시
- minutes의 현재 Topic taxonomy를 분류 선택지에 함께 사용
- 현재 검토/발행 상태는 브라우저 메모리 프로토타입이며 실제 공개/저장은 하지 않음

### 의도적으로 아직 하지 않은 것

- 실제 Archive 저장소 쓰기
- 회의 저장 직후 서버 측 자동 변환/발행
- 실제 관리자 인증
- resident 인증
- private 자료 전달
- 실제 검색 인덱스에 변환 결과 반영

이 항목들은 4단계 저장소·권한 구조와 연결해서 결정한다.

## 변경 파일

새 파일:
- `archive-v1/admin/stage3/meeting-source.js`
- `archive-v1/admin/stage3/meeting-adapter.js`
- `archive-v1/admin/views/meeting-import.js`
- `archive-v1/assets/admin-stage3.css`
- `archive-v1/progress-3-6.js`
- `docs/archive-v1/MEETING_ADAPTER_V1.md`
- `archive-v1/tests/fixtures/stage3-meetings.json`
- `archive-v1/tests/stage3-source.test.js`
- `archive-v1/tests/stage3-adapter.test.js`
- `archive-v1/tests/stage3-live-data.test.js`
- `archive-v1/progress-3-7.js`
- `docs/archive-v1/VALIDATION_POLICY.md`
- `docs/archive-v1/STAGE3_VALIDATION.md`
- `docs/CONTINUE_ARCHIVE_PROMPT.md`

수정:
- `archive-v1/admin/index.html`
- `archive-v1/admin/app.js`
- `archive-v1/admin/store.js`
- `archive-v1/admin/views/dashboard.js`
- `archive-v1/admin/views/classification.js`
- `archive-v1/admin/views/meeting.js`
- `archive-v1/index.html`
- `docs/archive-v1/ROADMAP_V1.md`
- `AGENTS.md`
- `docs/AI_WORKFLOW.md`

## 3단계 당시 다음 작업 기록 — 완료됨

1. 최신 main에서 이 문서와 `docs/CONTINUE_ARCHIVE_PROMPT.md`를 읽는다.
2. 아래 검증을 다시 실행해 3단계 체크포인트가 유지되는지 확인한다.
3. 3.7 상태가 `done`인지 확인하고, 완료돼 있으면 4.1 저장소 역할 분리 설계부터 시작한다.
4. `MINUTES-20260901-01` 예약 9건은 담당 해제 전까지 수정하지 않는다.

```bash
node archive-v1/tests/stage3-source.test.js
node archive-v1/tests/stage3-adapter.test.js
SANDLE_MINUTES_ROOT=/path/to/minutes node archive-v1/tests/stage3-live-data.test.js
```

## 최근 인계

### 2026-09-02 12:00:00 KST — Claude — 사용자 지정 ①②③ 완료

사용자가 자는 동안 순서대로 진행하라고 지정한 세 가지. 셋 다 끝났고 배포본에서 확인했다.

**① 안건 → 회의록 원문 연결** (`5fb13e0`, minutes `4ffb08c`)
- Archive의 안건 항목에서 「회의록 원문 열기 →」로 실제 회의를 연다. `/minutes/?open=<회의id>`.
- Archive는 회의록을 **복제하지 않고 찾아가게** 한다. 원문은 언제나 회의록 앱이 보여준다.
- 회의 id가 없으면 링크를 만들지 않는다 — 아무 데도 안 가는 링크는 없느니만 못하다.
- 왕복 확인: 주차장 운영규정 검토 → `m_mstb8o7gji6wa` → 제5기 2026년08월 임시 입주자대표회의가 열리고 주소에서 `?open`이 지워진다.

**② 공개 화면 로그인 자리** (`58fbcb9` → `26f7cc1` → `c1c13e9`, 로드맵 4.4a)
- 권한 규칙과 세션 판정은 만들어 놨는데 **비밀번호를 넣을 자리가 없었다.** 그 자리를 만들었다.
- **지금 로그인해도 보이는 자료는 늘지 않는다.** 내부공개·비공개를 내려주는 경로가 아직 없다(4.1). 그 사실을 화면에 그대로 적었다.
- **여기서 사고 2건을 냈다. 둘 다 배포본에 일부러 틀린 비밀번호를 넣어보다가 잡혔다.**
  1. `signIn`은 `{ok, role}`을 돌려주는데 그 값을 참/거짓으로 봤다. 객체는 언제나 참이라 **틀린 비밀번호에도 로그인된 것처럼 그려졌다.** 권한 화면이 실패를 성공처럼 보이는 쪽으로 틀린 것이라 제일 나쁜 종류다. `SandleLogin.역할()`로 떼어내고 `stage4-login` 검사를 붙였다.
  2. 고쳐서 배포했는데 같은 버그가 계속 보였다. `index.html`의 `?v=`를 안 올려 브라우저가 옛 파일을 쓰고 있었다.
- 지침 `docs/AI_WORKFLOW.md` **15절**에 확인 방법을 박아 뒀다. 성공 경로만 보면 이런 결함은 절대 안 잡힌다.

**③ 데이터 기준 시각 표시** (`c13dbb6`, 로드맵 4.6a)
- 화면 위에 "2026년 9월 1일(어제) 저장분까지 반영돼 있어. 오늘 저장한 건 내일 보여."
- 기준은 **사본을 만든 시각이 아니라 마지막으로 저장된 회의록의 `updatedAt` 최댓값**. 사본 만들기는 매일 돌지만 바뀐 게 없으면 파일을 새로 쓰지 않아, `generatedAt`은 "언제 확인했는지"를 말해주지 못한다. 마지막 저장 시각을 쓰면 사본 만들기가 며칠 멈춰도 숫자가 자라지 않아 그 자체로 티가 난다. 3일 넘으면 빨갛게 경고.

**함께 한 것** — 사용자 요청 「제목 옆에 다른 카테고리를 다 붙이거나 선택된 것만 크게」(`bb26b0e`): 주제 39개를 가나다순으로 다 늘어놓고 지금 보는 것만 크게·굵게 표시. 눌러서 바로 갈아탄다. 첫 화면으로 돌아갈 필요 없다.

**검증**: 배포본 브라우저 러너 **16/16 통과**(`stage4-login` 추가).

**다음 사람에게**: 남은 것은 4.3c · 4.3f(node가 없어 막힘) · 4.5 · 4.6b, 그리고 **내부공개·비공개 원본 전달**(사용자가 Drive에 올려야 시작 가능 — AI는 도구 정책상 로컬 파일을 Drive에 올릴 수 없다).

### 2026-09-02 06:40:00 KST — Claude — 「본문으로 짐작」 66건 분류 완료

주제 훑어보기 화면으로 **사용자와 함께** 미분류 안건을 정리했다. 자동 판정이 못 하거나 틀린 것만 골라 사람이 판단한 구간.

- **처리량 66건** — 기타 안건 38건(2026년 8 · 2024~25년 17 · 2016~21년 13) + 제목 있는 안건 28건(명백 17 + 본문 확인 11)
- 화면의 「못 찾음」이 **0**이 되었다.
- **사용자가 정한 기준**: “회의할 때 나눠서 했어야 했는데 하나에 몰아넣은 적이 많다” → **논의된 것은 다 붙인다.** 다만 「나온 것」은 *논의된 것*이지 *키워드가 걸린 것*이 아니다. 그 판별은 자동이 못 하므로 사람이 본문을 읽고 한다. 실제로 한 안건에 태그 8개가 붙은 것도 있다(2026-04-22 찬반투표 9건 목록).
- **왜 이 기준인가**: 목록이 깔끔한 것보다 **나중에 찾을 수 있는 것**이 중요하다. 주차 차단기 교체를 실제로 의결했는데 「주차」로 검색해 안 나오면 그게 더 나쁘다. 못 찾는 것 > 좀 많이 나오는 것.
- **자동이 크게 빗나간 예**(사람이 아니었으면 그대로 굳었을 것): 「208동 철쭉 고사목」→인사·노무, 「주민운동시설 철봉 설치」→주차, 「화해권고 결정문 이의신청」→LH·관리이관, 「종무식 실시」→경비·보안, 「청소용역 재계약 미흡」→도서관.
- **저장 전 두 가지를 코드로 막았다**: ① 분류표에 없는 이름(오타로 유령 태그가 생김) ② 계획 건수와 대상 건수 불일치(**한 칸씩 밀려 엉뚱한 안건에 붙는다** — 눈으로는 못 잡는다)

**겪은 것 — Apps Script 과부하**: 17건 저장이 231초 걸리고 2건이 실패했다(`<!DOCTYPE ...` HTML 오류 페이지 반환). 잠시 쉬었다 다시 누르니 7초에 끝났다. **실패한 것만 지역 저장에 남는 설계 덕분에 재시도가 그대로 됐다.** 진행 표시가 한동안 갱신되지 않아 멈춘 것처럼 보이는 문제는 남아 있다(실제로는 진행 중).

### 2026-09-02 05:20:00 KST — Claude — 관계 대상 찾기 + 관계 자동화 보류 결정

사용자 질문("관계 검토는 어려우니 자동으로 맡기고 싶은데 나중에 문제 생길까")에서 시작. 배포본 **14/14 통과**.

- **왜 관계는 태그와 다른가**: 태그가 틀리면 항목이 엉뚱한 주제에 있는 정도지만, **관계는 사실 주장**이다. “A는 B의 후속”이 틀리면 없던 인과를 만들어낸다. 화면에 그려져 있으면 그럴듯해 보여 알아채기도 어렵다. 이 기록은 절차 점검·민원에 쓰이므로 **틀린 연결을 근거로 뭔가를 주장하게 되는 것**이 진짜 위험이다.
- **`shared/reference-resolver.js` 신설.** 관계의 대상이 「공고 제2022-128호」라는 **글자**였다(실제 기록이 아님). 원칙은 찾는 것이 아니라 **모르면 만들지 않는 것**: 확정(딱 하나) / 후보(여럿 — 사람이 고름) / 없음(만들지 않음). 공고 번호는 발행 주체를 떼고 연도-번호로 대조, 연·월 의결은 같은 회의체로 좁히되 그 달에 정기·임시가 다 있으면 후보로만, 차수는 기수를 모르면 확정 안 함, 자기 자신은 가리키지 않음.
- **실측(안건 1,212건): 자동으로 이어지는 것 0건.**
  - 원문이 무언가를 가리키는 안건 **4건**뿐 (0.3%)
  - 그 4건이 가리키는 「공고 제2022-128호」가 **보관함에 없음**(보관함 56건은 대부분 선거·선관위 공고)
  - **재료가 없어서 자동화할 대상이 없는 것**이지 규칙이 부실한 것이 아니다. 이 모듈이 없었으면 닿지 않는 링크 4개가 생겼을 것이다.
- **결론(사용자와 합의)**: 관계 자동화는 **보류**. 대신 하자소송 8년 연쇄처럼 **사용자가 이미 아는 중요한 흐름 몇 개를 손으로 잇는다.** 1,212건을 다 잇는 것은 목표가 아니다. 관리사무소 일반 공고가 보관함에 쌓이면 재개한다.
- 커밋: `2293b56`

**다음**: 사용자가 「본문으로 짐작」 61건 분류 검토 중.

### 2026-09-02 04:40:00 KST — Claude — setTags 부분 수정 도입 체크포인트

사용자와 상의해 **레코드 분리 대신 서버 쪽 부분 수정**을 택했다. 배포본 **13/13 통과**, 실제 저장 확인.

- **GAS 백엔드 v3** — `setTags` 추가, 웹앱 **배포 버전 5**(배포 ID·URL 그대로). 사본 `SandleAPT/minutes` `scripts/gas/main-backend.gs` 동시 갱신.
- 서버가 시트의 **json 열과 updatedAt만** 쓰고 `date`·`name`은 읽지도 쓰지도 않는다. **2026-09-01의 날짜 유실(64건)이 구조적으로 불가능해졌다.**
- 왕복 3회(읽기·저장·재조회) → **1회**. 실측: 회의 3건·안건 4건 저장에 **13초**(회의당 약 4.3초). 종전 방식은 회의당 약 8초.
- 클라이언트는 `setTags`가 있으면 그 길, 없으면 **옛 길(레코드 전체 저장)로 되돌아간다.** 서버 배포가 되돌려져도 화면이 계속 동작한다. spec에 두 경로를 모두 넣었다.
- 실측 검증: 틀린 키·키 없음 → `admin_required`, 없는 회의 → `not found`, 정상 저장 후 **날짜·이름 그대로·태그 외 내용 완전 동일**, 되돌리면 json이 문자 단위로 원래와 일치.
- 화면 확인: 저장 후 「이미 정해둠」 250 → 254로 즉시 반영.
- 커밋: `8a8ba6c`(GAS v3·사본), `38cfb24`(클라이언트 전환)

**함정 기록**: 배포 직후 1분쯤은 옛 버전이 응답한다(첫 시험에서 `unauthorized`가 나와 갸웃했으나 전파 지연이었다). 그리고 배포는 반드시 **「배포 관리 → 연필 → 새 버전」** — 「새 배포」를 누르면 새 URL이 생겨 앱이 옛 서버를 계속 본다.

### 2026-09-02 03:50:00 KST — Claude — 2.4g 회의록에 태그 저장 체크포인트

사용자 요청("2.4g 저장 버튼 붙여줘"). 배포본 **13/13 통과**, 실제 클라우드 저장까지 확인.

- `shared/tag-writer.js` 신설. 2026-09-02 03:10에 3건으로 직접 확인한 절차를 그대로 코드로 옮겼다. **네트워크를 주입받게** 만들어 실제 저장 없이 흐름 전체를 검증한다.
- 모듈에 박아 둔 규칙: ① `date`를 반드시 실어 보내고 없으면 **저장하지 않는다**(9/1에 이 자리에서 회의 64건의 date가 지워졌다) ② 재조회 검증은 문자열이 아니라 **파싱한 내용**으로 대조(원본은 콜론 뒤 공백이 있어 문자열 비교하면 멀쩡한 저장도 실패로 잡힌다) ③ 태그 말고 달라진 곳이 있으면 저장 전에 멈춘다 ④ 빈 태그로 덮어쓰지 않는다 ⑤ 회의 하나가 실패해도 나머지는 계속하고 **성공한 것만** 지역 저장에서 지운다.
- 실제 확인: 「전체세대 주민등록등본 취합 검토」(헬스장 → **선거·임원**) 저장 성공. 클라우드에서 태그·날짜·안건 수 12건 모두 정상.
- **눌러 보고 고친 것 둘** — 이 둘은 spec으로는 안 잡히는 종류였다.
  1. 저장 뒤 화면을 다시 읽으니 **옛 값이 돌아왔다.** 이 화면은 `/minutes/data-YYYY.json` 정적 사본을 읽는데 그 파일은 하루 한 번 다시 만들어진다(`4.6` 신선도). 저장한 값을 화면 자료에 그대로 반영하도록 바꿨다. 사본은 다음 재발행 때 따라온다.
  2. 저장이 멈춘 줄 알고 파고들었더니 Apps Script 읽기 한 번이 24초 걸리고 있었다. **다만 이 24초는 평상시 값이 아니다** — 그날 종일 GAS를 두들긴 직후의 값이었고, 잠잠할 때 다시 재니 **평균 2.3초**(1.8~2.7초, 5회)였다. 회의당 왕복 세 번이므로 **평상시 약 8초/회의**로 본다. 진행 표시에 경과 시간을 넣었다.
- 커밋: `03ed5c0`(모듈·버튼·spec), `b8a8684`(저장 후 반영·진행 표시)

**후속**: 여기서 지적한 「레코드 전체를 다시 쓰는 구조」는 2026-09-02 04:40 체크포인트에서 `setTags` 부분 수정으로 해결했다.

### 2026-09-02 02:20:00 KST — Claude — 주제 훑어보기: 회의록 태그 반영 체크포인트

사용자 결정(“회의록에 직접 쓰고 그걸 활용해서 쓰는 게 맞지 않아? 결국 태그는 양쪽 다 쓰는 거니”)에 따라 **회의록 태그를 단일 출처로 삼기로** 했다. 쓰기를 붙이기 전에 읽기 쪽 결함 세 가지를 먼저 잡았다. 배포본 **12/12 통과**.

- **① 이미 붙은 수동 태그 250건을 무시하고 있었다.** 사람이 정해둔 값을 화면이 자동 판정으로 덮어 보여줬다. 그 상태로 저장을 붙였다면 250건이 자동 판정으로 되돌아갔을 것이다. 우선순위를 회의록 앱과 맞췄다: 방금 고침 > 회의록 수동 태그 > 자동 판정.
- **② 태그는 여러 개인데 화면은 하나만 고르게 돼 있었다.** 실제로 「관리규약·잡수입·예산·전기·설비」처럼 셋이 붙은 안건이 있다. 저장했다면 나머지를 지웠을 것이다. 칩으로 빼고 드롭다운으로 더하는 방식으로 바꿨고, 모두 없앨 수는 없게 막았다.
- **③ 옛 태그 이름이 그대로 떴다**(`소송`·`미화`). v98에서 `하자·소송`·`청소·미화`로 통합한 것들이다. 회의록 앱의 `TopicTaxonomy.resolveStored`를 그대로 써서 푼다 — 이름 변환 + `기타`·`저수조·청소` 같은 포괄 태그의 자동 보강까지 같은 규칙이 된다.
- 화면 확인: 칩 합계가 1,212와 정확히 맞고(못 찾음 1 · 본문으로 짐작 65 · 여러 주제 325 · 안건명 판정 571 · 이미 정해둠 250), 옛 이름은 0건.
- 커밋: `036916c`(수동 태그·다중 태그), `ee55819`(칩 누락), `a22128d`(resolveStored)

**다음: 회의록에 실제로 쓰기.** 아직 안 붙였다. 붙이기 전에 갖춰야 할 것 —
1. **수정용 비밀번호**가 필요하다(회의록 클라우드 저장).
2. **저장 단위는 회의 레코드 하나**다. 안건 태그만 바꾸려면 그 회의의 json 전체를 다시 저장해야 하므로, 읽은 뒤 다른 필드를 건드리지 않았는지 대조해야 한다.
3. **`date`를 반드시 실어 보낼 것** — 2026-09-01 사고와 같은 자리다(`minutes/docs/DATA.md`).
4. 재조회 검증에 `json`과 `date`를 함께 대조한다.
5. 한 번에 다 쓰지 말고 몇 건 먼저 쓰고 회의록 앱에서 눈으로 확인한 뒤 나머지를 진행한다.

### 2026-09-02 01:40:10 KST — Claude — 2.4 주제 훑어보기 화면 체크포인트

사용자 요청("그 검토 화면 만들어줘"). 배포본 **12/12 통과**, 실제 화면에서 동작 확인.

- **승인 게이트가 아니다.** 분류는 기록당 한 번만 하면 되는 일이라, 한 건씩 막고 승인받는 대신 실제 안건 **1,212건을 전부 펼쳐 놓고** 이상한 것만 고치는 화면으로 만들었다.
- **확인이 급한 것부터 위로**: 못 찾음(16) → 본문으로 짐작(80) → 여러 주제(402) → 안건명으로 판정(714). 사람이 고친 것은 맨 위로 온다.
- 상태·주제·검색으로 거르고, 주제를 바로 바꾼다. 200건씩 끊어 보여주고 「더 보기」로 늘린다.
- **화면을 열자마자 오분류가 드러났다**: 「전체세대 주민등록등본 취합 검토」→ 헬스장, 「입주민 찬반투표 진행 건」→ 주차. 전부 ‘본문으로 짐작’ 묶음에 있다. 이 묶음을 맨 위로 올린 판단이 맞았다.
- 새 파일: `shared/topic-review-model.js`(줄 만들기·정렬·거르기 — spec 대상), `admin/views/topic-review.js`(화면), `assets/admin-topic-review.css`, `tests/specs/stage4-topic-review.spec.js`
- 자료는 `/minutes/data-YYYY.json` 정적 사본에서 읽는다(같은 origin, 읽기 전용).
- 커밋: `994c50b`

**미결(사용자 확인 필요)**: 고친 주제를 **어디에 저장할 것인가.** 지금은 이 브라우저의 `localStorage`에만 남는다. 회의록 앱은 안건별 수동 태그(`a.tags`)를 이미 지원하므로 그쪽에 쓰는 것이 자연스럽지만, **Archive에서 회의록 원본에 쓰는 것은 「원본 덮어쓰기」에 해당**해 사용자 결정 없이 하지 않는다.

### 2026-09-02 01:05:20 KST — Claude — 자동 판정 실측·보정 체크포인트

앞 체크포인트의 남은 과제(“실제 1,125건에 돌려 애매 비율 확인”)를 실행했다. 배포본 **11/11 통과**.

- **실측(실제 안건 1,212건)**: 제목과 본문을 함께 채점하는 방식은 **61.2%(742건)가 사람에게** 넘어와 자동화가 무의미했다. 원인은 본문에 논의 내용이 통째로 들어 있어 제목을 덮은 것. 실제로 「잡수입 보고의 건」이 경비·보안으로, 「주차장 운영규정(안)」이 장기수선으로 분류됐다.
- **회의록 앱의 규칙을 그대로 채택**: `archive-topics.js`의 `autoTags`는 **제목을 먼저 보고 걸리면 본문을 아예 보지 않는다.** 사용자가 v98에서 1,125건을 검토한 결과가 그 규칙 위에 쌓여 있으므로 Archive가 다른 규칙을 쓰면 두 앱이 같은 안건에 다른 주제를 붙인다. 대표 주제 선택 순서(분류표 순서)까지 맞췄고 spec으로 고정했다.
- **결과**: 제목 판정 **92.1%**(1,116건, 그중 402건은 여러 주제에 해당 — 정상), 본문만 6.6%(80건), 못 찾음 1.3%(16건). **사람이 볼 것 7.9%(96건).**
- **사용자 지적 “제목이랑 내용이 안 맞는 경우도 있다”**: 자동 대조를 두 방식으로 시도했으나 **키워드 대조로는 잡히지 않는다**고 판단했다. ‘본문이 제목에 없는 주제를 언급’은 41.7%, ‘제목 주제가 본문에 안 나옴’은 44.4%가 걸리는데 표본을 보면 대부분 오탐이다(예산 안건 본문에 주차·경비가 나오는 것은 정상, 의결문이 짧으면 제목 단어가 본문에 다시 안 나오는 것도 정상). **불일치 탐지는 자동화하지 않는다.**
- 검증이 잡은 것 2건: ① `relate()`가 삭제된 `textOf`를 부르고 있었다 ② **러너의 비격리 경로에 캐시 무효화가 없어** 배포된 새 모듈을 두고 캐시된 옛 파일로 검증하고 있었다(이미 고친 오류가 계속 실패로 나옴). 검증이 옛 파일을 보면 통과도 실패도 믿을 수 없으므로 러너를 고쳤다.
- 커밋: `45c3dd0`(제목 우선), `d4d9f60`(textOf 수정), `242de2b`(러너 캐시)

**다음**: 분류는 기록당 한 번만 하면 되는 일이라(사용자 지적) 정확도를 택할 여지가 있다. 승인 게이트 대신 **제목·본문·붙은 주제를 한눈에 훑어보고 고치는 검토 화면**이 다음 후보다.

### 2026-09-01 23:35:40 KST — Claude — 2.4 자동 판정 + 연결 유출 차단 체크포인트

사용자 검토(`4.4`)가 시작되어 나온 피드백을 반영한 묶음이다. 배포본에서 **11/11 통과**.

- **관계 문구**: “관계종류가 이해 안 된다 — 영어라서가 아니라 무슨 역할인지 몰라서”. 번역이 아니라 화면의 목적을 설명하는 문제였다. 하자소송 8년 연쇄를 예시로 넣고, 고른 값의 뜻과 저장 결과를 미리 보여준다. `shared/relation-labels.js`
- **`verified`/`inferred` 구분**을 ‘확인 작업을 실제로 했는가’로 명문화. “아마 맞을 것 같다”는 inferred다. inferred는 화면에서 붉게 표시한다.
- **자동 판정**(`shared/auto-assign.js`): 회의록 앱의 38종 분류표를 재사용한다. 첫 일치로 끝내지 않고 모든 주제를 채점해, **1·2등이 맞붙으면 사람에게 넘긴다.** 자동으로 넘어간 것은 아무도 다시 보지 않으므로 이 선별이 핵심이다.
- **관계 자동 생성 결함 수정**: 이전 구현은 문서 종류만 보고 `target`에 ‘근거 회의·결정 확인 필요’ 같은 안내문을 넣은 관계를 만들었다. 공고가 들어올 때마다 가리키는 대상 없는 항목이 쌓이고 매번 ‘연결 없이 진행’을 눌러야 했다. 할 일 메모를 관계인 척 저장한 셈이라 폐지하고, **원문이 실제로 가리킬 때만** 만든다.
- **연결 유출 차단**: `filterRelations`/`projectRecord` 신설. 볼 수 없는 상대와의 연결은 흔적 없이 없앤다. 등급만 가리면 무엇이 감춰져 있는지가 제목째 새어 나간다.
- 검증이 잡은 것: 이번엔 **spec의 기대값이 틀렸다**(조회 함수 누락 시 관리자는 비공개를 볼 수 있으므로 그대로 보이는 게 맞다). 코드가 아니라 단언을 고쳤다.
- 커밋: `7ab26c3`(관계 문구), `5000bce`(배지·미리보기), `486d26b`(자동 판정·유출 차단), `b9028b0`(spec 정정)

남은 것: 자동 판정을 **실제 회의록 1,125건에 돌려** 애매 판정 비율이 실제로 쓸 만한지 확인해야 한다. 지금은 표본으로만 검증했다.

### 2026-09-01 20:48:15 KST — Claude — 4.3b 인증·접근 판정 체크포인트

- 사용자가 인증 방식을 결정함(회의록 비밀번호 재사용). 결정 내용은 「사용자 결정 기록」 표와 `docs/archive-v1/AUTH_V1.md`에 남김.
- 세션 모듈(`shared/auth-session.js`)과 판정 모듈(`shared/access-control.js`)을 분리해 구현. 판정 규칙은 한 곳에만 둠.
- 검증 spec 2종 추가, 배포본에서 **9/9 통과**(Stage 3 3종 + Stage 4 6종, 실제 데이터 213회의/1,125안건).
- **검증이 결함을 잡음**: `currentRole()`이 캐시된 role을 무기한 믿어, 비밀번호를 회수해도 그 브라우저가 24시간 통과하는 상태였음. 재확인 주기 5분을 도입해 회수 반영을 최대 5분으로 줄임.
- `stage4-auth-session`은 인증 키를 조작하므로(같은 origin이라 회의록 로그인까지 영향) 원래 값을 백업·복구하도록 만들고, 실행 후 상태가 보존되는 것을 확인함.
- 새 파일: `archive-v1/shared/auth-session.js`, `access-control.js`, `tests/specs/stage4-access-control.spec.js`, `stage4-auth-session.spec.js`, `docs/archive-v1/AUTH_V1.md`
- 수정: `archive-v1/tests/browser/index.html`, `docs/AI_WORKFLOW.md`(13절 결정 기록 규칙), `ROADMAP_V1.md`, `STAGE4_VALIDATION.md`, 이 문서
- 운영 루트·minutes 원본·공개 범위 변경 없음. 정적 번들에는 여전히 `public`만 들어간다.
- 커밋: `70a1deb`(설계·결정 기록), `cf78d5d`(구현·spec), `b9ddf42`(캐시 결함 수정)

### 2026-09-01 19:20:05 KST — Claude — 4.3g 실제 데이터 전량 검증 체크포인트

- `stage3-live-data` spec을 브라우저로 이식해 **7/7 통과**(회의 213건 · Fragment 1,125건 · 안건수 불일치 0 · ID 중복 0).
- 건수를 고정하지 않고 구조 불변조건만 검사하도록 바꿈. 회의록이 계속 늘어나기 때문.
- **발견: Archive가 읽는 정적 샤드가 클라우드보다 뒤처진다.** 클라우드 224건 / 정적 샤드 213건이며 차이 11건은 전부 07:18 KST 이후 적재된 입대의 1기 회의록이다. 손실이 아니라 봇 재발행 지연 — 다음 재발행 후 224건으로 늘어난 채 통과하는 것이 정상이다. 후속 검토를 `4.6`으로 등록함.
- 러너 보완 2건: 격리 iframe의 경로 해석(`ctx.origin` 제공), 격리 로드 캐시 무효화(옛 spec 재사용으로 수정이 반영되지 않던 문제).
- 새 파일: `archive-v1/tests/specs/stage3-live-data.spec.js`
- 수정: `archive-v1/tests/browser/runner.js`, `index.html`, `docs/archive-v1/ROADMAP_V1.md`, `STAGE4_VALIDATION.md`, `docs/WORK_STATUS.md`
- 운영 루트·minutes 원본·기존 node 테스트 변경 없음. 공개 범위 변경 없음.
- 커밋: `94b26d4`(spec), `096c18a`(경로 해석), `9a06ea2`(캐시 무효화), 문서는 이 인계와 함께 반영.

### 2026-09-01 18:22:40 KST — Claude — 4.3e Stage 3 spec 이식 체크포인트

- `stage3-source`, `stage3-adapter` spec을 추가해 브라우저 러너에서 **6/6 통과**(실패 0).
- 러너에 **iframe 격리 실행**과 **setup 단계**를 추가함. spec이 fetch 스텁·모듈 내부 캐시·전역 스텁을 건드려도 서로 오염되지 않는다.
- 격리 필요성을 실증함: 격리를 끄고 같은 spec을 두 번 돌리면 2회차가 `r.text is not a function`으로 실패한다.
- **사고 있었음** — 러너 페이지 캐시 버전을 PowerShell로 바꾸다 한글이 전부 깨진 채 커밋·푸시됨(`4badffe`). `git checkout`으로 복구 후 편집 도구로 다시 수정해 `459240f`로 정상화함. 재발 방지 규칙을 `docs/AI_WORKFLOW.md` 12절에 추가함.
- 새 파일: `archive-v1/tests/specs/stage3-source.spec.js`, `stage3-adapter.spec.js`
- 수정: `archive-v1/tests/browser/runner.js`, `archive-v1/tests/browser/index.html`, `docs/AI_WORKFLOW.md`, `docs/archive-v1/ROADMAP_V1.md`, `docs/archive-v1/STAGE4_VALIDATION.md`
- 운영 루트·minutes 원본·기존 node 테스트 변경 없음. 공개 범위 변경 없음.
- 남은 위험: `stage3-live-data` 미이식(4.3g), 케이스 이중 관리(4.3f).
- 커밋: `4badffe`(spec 추가, 인코딩 손상), `459240f`(복구·등록), 문서는 이 인계와 함께 반영.

### 2026-09-01 17:34:20 KST — Claude — 4.3d 브라우저 검증 러너 체크포인트

- 사용자 지시로 ARCHIVE-20260901-01 담당이 GPT → Claude로 넘어옴. `MINUTES-20260901-01` 예약 9건은 완료 처리하고 잠금 해제함.
- **인계된 검증 명령이 이 환경에서 실행 불가**임을 확인함: 사용자 PC에 Node.js 없음. 이 상태로는 자동 검증 없이 `done` 판정을 할 수 없어 먼저 검증 경로를 만듦.
- 케이스를 UMD spec 모듈로 분리하고 브라우저 러너를 추가함. 기능·스타일·테스트·문서를 각각 다른 파일로 나눔.
- **기존 node 테스트 파일은 수정하지 않음.** node로 회귀 확인이 불가능한 환경에서 건드리면 위험하다고 판단함.
- 검증: 실제 배포본 `/archive-v1/tests/browser/`에서 Stage 4 spec 4종 전부 통과(4/4, 실패 0).
- 러너 신뢰성도 별도 확인함 — 정책을 일부러 되돌려 회귀를 만들었을 때 실패로 잡히고, 원복 후 다시 통과함.
- 새 파일: `archive-v1/tests/specs/assert.js`, `stage4-source-reference.spec.js`, `stage4-visibility.spec.js`, `stage4-publish-guard.spec.js`, `stage4-admin-integration.spec.js`, `archive-v1/tests/browser/runner.js`, `archive-v1/tests/browser/index.html`, `archive-v1/assets/test-runner.css`
- 수정: `docs/archive-v1/ROADMAP_V1.md`, `docs/archive-v1/STAGE4_VALIDATION.md`, `docs/WORK_STATUS.md`
- 운영 루트·minutes 원본·기존 node 테스트 변경 없음. 공개 범위 변경 없음.
- 남은 위험: Stage 3 spec 미이식(4.3e), 케이스가 spec과 node 테스트 두 곳에 존재(4.3f).
- 체크포인트 커밋: `6374951`(러너), 문서 커밋은 이 인계와 함께 반영.

### 2026-09-01 15:32:08 KST — GPT — 4.3a 공개 projection·발행 차단 체크포인트

- 별도 `stage4/` 정책 모듈과 별도 Stage 4 화면·스타일·테스트 파일로 나눠 구현함.
- 공개 포털·minutes·외부 원본 저장소의 역할을 관리 화면에서 확인할 수 있게 함.
- public 자료는 허용 필드 projection을 만들고 resident/private는 공개 발행을 차단함.
- 발행 화면뿐 아니라 store에서도 같은 정책을 강제하고 모듈 누락 시 fail-closed 처리함.
- 공개 Archive 진행판을 4.3으로 갱신하고 캐시 버전을 `20260901-152900`으로 올림.
- 자동 검증: Stage 3 회귀 213회의/1,125안건, Stage 4 테스트 4개, 전체 JS 문법, diff 검사 통과.
- 헤드리스 Chromium 부재로 브라우저 클릭 자동화는 실행하지 못했으며 이 제한을 `STAGE4_VALIDATION.md`에 기록함.
- 기존 운영 루트, minutes 원본, Claude 예약 9건 변경 없음.
- 실제 resident/admin 인증은 사용자 결정이 필요하므로 4.3b로 남김.
- 구현·원격 반영 커밋: `6bb6e633900e8d6217fcc4a20bd0b8a86b7aa223`
- 원격 반영 완료: `2026-09-01 15:32:08 KST`

### 2026-09-01 15:20:28 KST — GPT — 4.1~4.2 저장소·원본 참조 체크포인트

- 운영 루트 변경 없이 Archive 전용 저장·권한 구조 작업을 시작함.
- GitHub 포털, minutes 원본, 외부 원본 저장소의 역할을 분리해 문서화함.
- provider에 종속되지 않는 `SourceReference v1`을 구현함.
- 공개 번들은 허용 필드만 새로 만드는 projection 방식으로 구현함.
- resident/private 자료와 외부 `file_id`가 공개 번들에 들어가지 않도록 기본 정책을 구현함.
- visibility 누락·오류는 public이 아닌 `private`로 처리함.
- 자동검증: `stage4-source-reference`, `stage4-visibility` 통과.
- 새 파일: `archive-v1/admin/stage4/source-reference.js`, `visibility-policy.js`, Stage 4 테스트 2개, `STORAGE_AND_ACCESS_V1.md`.
- 기존 운영 루트, minutes 원본, Claude 예약 9건 변경 없음.
- 다음: 4.3 정책을 관리자 공개등급·발행 흐름에 연결.

### 2026-09-01 14:37:11 KST — GPT — 3단계 자율 검증 체크포인트

- 단계별 사용자 승인 대기 대신 담당 AI 자율 검증·보완 후 진행하는 규칙으로 전환함.
- 긴 작업 중단에 대비해 기능 묶음마다 중간 체크포인트를 남기는 규칙을 추가함.
- 다른 AI용 이어하기 프롬프트를 `docs/CONTINUE_ARCHIVE_PROMPT.md`에 추가함.
- 실제 회의 213건, 안건 1,125건 전체 변환을 검증함.
- 과거 분류명과 `기타` 정규화 문제를 보완함.
- 자동 검증 fixture와 3개 검증 스크립트를 별도 파일로 추가함.
- 기존 minutes 원본, 운영 루트, Claude 예약 9건은 수정하지 않음.
- 코드·규칙 체크포인트 커밋: `bba7a5d40f53eab88833184cbf0b9d0f6e2f66d6`
- UI·진행상태 체크포인트는 이 인계 직후 main에 반영함.
- 3단계 완료 시각: `2026-09-01 15:15:51 KST`
- 다음: 4.1 저장소 역할 분리 설계.

### 2026-09-01 14:22:30 KST — GPT — 3.1~3.5 프로토타입 구현 / 3.6 사용자 검토 대기

- 사용자가 3단계 전체 진행을 승인함.
- 기존 minutes를 수정하지 않고 read-only adapter 방식으로 연결함.
- 실제 연도별 회의 데이터를 선택해서 Document/Fragment 변환 결과를 볼 수 있게 함.
- 기존 태그와 자동 주제 후보를 구분하고, 의결·후속조치·표결을 구조화함.
- 변환된 안건을 기존 관리자 분류 검토 흐름으로 보낼 수 있게 연결함.
- 기존 1페이지 회의록 출력은 minutes에 그대로 남김.
- 공개 Archive의 진행상황도 3.6 검토로 변경함.
- Claude 예약 9건과 운영 루트는 수정하지 않음.
- 관련 커밋: `0ef9e97ef93393a37183b2a67b16a283e2c63fc5`, `0becb5e588f44acc359df93a3e4a289b644b442f`, `b2497833d1862840f3de670e4d9324b25b9af1bf`, `cb5e7df13c9079fe874006738bb0c10ab058fec4`, `f09752dd729a3e336def4555f38e30e731b043f4`, `909eaea85b947d2544d5825a4f17943e88ebad53`, `552de47e850c75fa52ec410b8a03207c020a4fda`, `ec509bbac713d3af56b3a7506cb855d9f2cde628`, `cffb298ef32d83da9f0bb6b2399f926ee1385a90`, `96d1f387ae38a0f0ed9da068b9fdc6b45606472f`, `863c6f240401844404df0164cfc0495817e4bdc0`, `e34b096fdd90816d9d3526033406e4c7666ac9ef`, `d7b34327b99474f6f4cd6b80ca0a16c633fcfa40`, `e8e574590c11837aafc3ef82a460db25d80552e5`, `ab59c804c4ab2591748465217f8b60ef10cbe8c6`.

### 2026-09-01 14:02:45 KST — GPT — 2단계 테스트 가능 상태 보강

- 2.2 / 2.3 / 2.4를 번호별로 직접 테스트할 수 있게 보강함.
- 2단계는 실제 사용 중 추가 수정할 수 있도록 `review` 상태로 유지함.

## 기록 규칙

작업 종료 시 현재 번호, 완료 번호, 다음 번호, 변경 파일, 주의사항, 완료 시각 `YYYY-MM-DD HH:mm:ss KST`, 관련 커밋을 남긴다.
